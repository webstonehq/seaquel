# Phase 1 effort log (task | wall time | Rust lines added | TS lines removed | surprises)

- Task 1 | ~4 min | +385 src (dialect.rs 382, lib.rs 3) +418 tests | −265 / +42 in the 4 type files (net −223; +8 in index.ts barrel) | None serious. ts-rs honours the serde renames (`type`, kebab-case, "Date/Time"/"JSON"/"UUID") with no ts attrs. `Option<i64>` fields use `ts(as = "Option<f64>")` so optional_fields still emits `field?: number`. svelte-check found no inconsistencies. `actualRows`/`planRows` are f64 because PG 18 reports fractional actual rows.
- Task 2 | ~35 min incl. review fixes | +~410 src (value.rs ~250, three sqlx bind.rs 98, rest is signature churn; MSSQL/DuckDB binders net −16) +~290 tests (tests/value.rs, PG exact-int test 26, testkit checks) | −5 TS removed (crud-helpers interface → generated re-export, `params ?? []` sites); +165 values.ts, +165 values.test.ts, +30 json-export.test.ts, ~20 lines of provider/export/clipboard wiring | A first cut turned integral floats outside the i64 range into `Decimal` (to meet "plain 2^64 → Decimal"), but that made driver float cells like `1e20` look like exact decimals. Now any number serde_json parses as a float stays `Float`; only u64 above i64 becomes `Decimal` (plan text updated). The `bind_fn` binder is per-value and fallible, so arrays fail cleanly (QUERY_ERROR) in `query_stream`/`transaction` too. `encodeParam` also tags NaN/±Infinity and passes `Date` through untagged. The testkit gates the array-param check behind `SmokeSpec.supports_array_params` (false everywhere; Task 7 flips it for Postgres). JSON export and copy-row use `jsonReplacer` already so bigint rows can't crash them before Task 11.
- Task 3 | ~30 min | +0 Rust (fixtures only: 12 recorded JSON files, 110 cases, plus hand-written bugfixes.json with 11 cases) | −0 TS; +~1160 recorder/checker (scripts/fixtures/postgres-dialect.fixtures.test.ts), +@types/pg devDependency, scripts/fixtures/tsconfig.json so the type-aware lint resolves `$lib` | Found a fifth TS bug outside the four fixes: the three statistics queries splice `schemaname || '.' || relname` into regclass lookups, so statistics fail with "invalid name syntax" for any table whose name needs quoting (spaces, hyphens, upper case). The recorder drops its `order items`/`my-table` scratch tables before recording stats; the Rust port inherits the bug unless it's added to the fix list. `pg_get_indexdef(…, k, true)` (pretty) prints `lower(name::text)`, not the plan's `lower((name)::text)`. DROP INDEX cases live only in bugfixes.json (fix 3), since TS's unqualified output can't be a parity target. `postgres.ts` can't be imported first (circular import with `db/index.ts`); the recorder imports `$lib/db` first.
- Task 4 | ~25 min | +~640 src (ddl.rs 360, crud.rs 166, dialect.rs 65, lib.rs ~50 for Engine::dialect + five NOT_SUPPORTED introspection defaults) +~845 tests (ddl 17 tests, crud 12 tests, lib 3 tests) | −0 TS (the TS builders stay until Task 13) | Port was mechanical; a throwaway harness ran all 62 crud/ddl-create/ddl-alter/bugfix-DDL fixture cases through the generic builders (Postgres-like quote fns, `qualify_drop_index` for fix 3) and all matched. Two things for later tasks: (1) serde_json here has no `preserve_order`, so any JSON object → `RowValues` decode sorts keys; the fixture loader in Task 5 and the RPC in Task 9 must keep insert column order (pairs on the wire, or `preserve_order`). JS itself hoists integer-like keys, which the Rust port doesn't reproduce. (2) JS `trim()` ≠ Rust `trim()` (BOM, NEL), so `sanitize_default_value` uses a JS-exact trim.
- Task 5 | ~15 min | +~190 src (dialect.rs 184, lib.rs 6, Cargo.toml +1 dep on seaquel-types) +~400 tests (tests/dialect_parity.rs: 8 tests, 78 fixture cases) | −0 TS (postgres.ts goes in Task 13) | All 78 cases passed on the first run. The red step failed at runtime, not at compile time: `Engine::dialect()` already had a `None` default from Task 4, so the parity test compiled against the public trait. Insert order comes from a small order-preserving `Deserialize` for the fixture `values`/`row` objects instead of turning on `serde_json/preserve_order`, because Cargo feature unification would flip key order for the whole test build. Watch out: `cargo fmt -p` reformats files other tasks left unformatted (the workspace isn't rustfmt-clean), so I restored those hunks by hand.
- Task 6 | ~30 min | +~485 src (introspect.rs 419: 7 SQL consts + parsers; driver.rs +57 for the five Driver methods; impl_sqlx_driver! +7 for an optional `introspection = { … }` block; futures dep) +~670 tests (tests/introspect_parity.rs 400: 8 tests over 30 recorded cases, EXPLAIN run 3× per case as Array/Json/Text cells; testkit run_introspection + scratch_name +144; smoke.rs `introspection` +123) | −0 TS (postgres.ts goes in Task 13) | Parity cases that caught a real difference while porting: 0 of 30 (parse-schema 1, parse-columns 10, parse-stats 4, parse-explain 8, sql 7); all passed on the first green run, and so did the live fix 1/4/5 checks. Notes: (1) only the table-sizes query splices `schemaname || '.' || relname`; index usage already uses `indexrelid` and the overview has no per-table lookup, so "fix 5 for three queries" is one query in practice. (2) TS `parseExplainResult` throws on zero rows (`undefined[0]`); the Rust port returns an `Unknown` root with planning time 0, as the plan asks. (3) The generated `Driver` impl lives in `impl_sqlx_driver!`, so engine-specific methods needed a hook in the macro. (4) The live test renames the `bugfixes.json` scratch schema to a unique `seaquel_introspect_<uuid>` (a text replace over the fixture), so it can't collide with the recorder or a parallel run. Review follow-up (~10 min, +~60 lines): plain index key columns now come back as `pg_attribute.attname` (unquoted, so `"Mixed Case"` → `Mixed Case`, matching `SchemaColumn.name`; `pg_get_indexdef` alone would have returned the quoted form and the DDL generator would quote it twice) with a live test case; `run_introspection` reports teardown failures on stderr, drops stale `seaquel_introspect_%` schemas before setup (a `DO` loop; risky only if two runs share one database), and checks index usage by a known index name.
- Task 7 | ~35 min | +~490 src (numeric.rs 363 incl. 4 unit tests: own NUMERIC binary codec; bind.rs +126 for arrays and exact decimals; decode.rs net −117 after rewriting to `Value` with small helpers) +~170 tests (smoke.rs `values_round_trip`: 26 literals decoded and re-bound, plus float NaN, json, client decimals, `= ANY`, mixed arrays, information_schema domains; testkit positive array branch +17) | −0 TS | (1) sqlx 0.8's `PgNumeric` is `pub(crate)` and both `rust_decimal` and `bigdecimal` lose NaN/Infinity (and rust_decimal >28 digits, bigdecimal the display scale), so NUMERIC is read and written in the binary wire format by hand; no sqlx feature changes. (2) The old decoder panicked (rust_decimal "Multiplication overflowed") on `123456789012345678901234567890.123::numeric`, turned NaN floats/numerics into null, and returned null for any array containing a NULL; DATE[]/BYTEA[]/JSON[] came back as raw binary garbage text. (3) Postgres has no `int8[] = int4[]`, so the plan's `SELECT $1 = ARRAY[1,2]` forced integer arrays to bind as INT4[] when every element fits and INT8[] otherwise. (4) `json` has no `=` operator, so the json round trip compares against jsonb. Review fixes (+~35 min, +~110 Rust incl. tests): (5) the big one was sqlx's per-connection statement cache, keyed by SQL text alone, which reuses the first prepare's parameter types. Once Int and Float bind as different types, the same SQL run later with a Float writes its bits as an INT8 (1.5 became 4609434218613702656), and Text after Int failed on Postgres and came back as `Int(0)` on MySQL. `impl_sqlx_driver!` now marks every statement `persistent(false)`, and `run_smoke` alternates Int/Float/Text on one SQL for 20 rounds on every engine. MSSQL (tiberius `Query::new`, sp_executesql each call) and DuckDB (`prepare`, not `prepare_cached`) don't cache and pass. (6) Scalar UUID cells decoded as **Null** before this task (sqlx's `uuid` feature is off and a UUID isn't a `String` to sqlx), so UUID and UUID[] now use a 16-byte decoder. Arrays with no decoder, multi-dimensional arrays and arrays that don't start at 1 are now `<unsupported: T[]>` text, not binary garbage or Null.
- Task 8 | ~15 min | +~90 src (lib.rs: `Connection { engine, driver }` map value, `engine`, `with_dialect`, five introspection pass-throughs) +~300 tests (tests/mock.rs: MockDialect, IntrospectingDriver, a "postgres" DialectEngine, 6 tests) | −0 TS | The plan's `Core::dialect(&self, id) -> Result<&dyn Dialect, _>` can't borrow past the connections lock, so Core hands out `engine(id) -> Arc<dyn Engine>` plus `with_dialect(id, |d| …) -> Result<R, DbError>` (NOT_SUPPORTED when the engine has none); the closure form is one line per `EngineRequest` variant in Task 9's dispatcher. `connect` now looks the engine up itself (`registry.get` + `engine.open`) instead of `EngineRegistry::open`, so it can keep the `Arc<dyn Engine>`; `test` still uses `EngineRegistry::open`. No lock is held across an await (both new accessors clone out of the map).
- Task 9 | ~30 min | +~230 src (seaquel-rpc lib.rs 210 incl. docs; server route 14 + NOT_SUPPORTED→501 mapping; Tauri `db_engine` 7) +~790 tests (tests/dispatch.rs 608: 19 tests, one per variant plus NOT_SUPPORTED/CONNECTION_NOT_FOUND over all 13 variants, malformed input, serde round trip; server db_engine.rs 128: 3 tests + error.rs status unit test; proxy.test.ts 97: 4 tests) | −0 TS | (1) `with_default_plugins()` can't be used in the rpc tests: Cargo unifies seaquel-core's features across the workspace, so in `cargo test --workspace` it already registers "postgres" and adding the mock "postgres" engine panics as a duplicate; the tests build the Core from `seaquel_engine_sqlite::engine()` plus the mock explicitly. (2) Clippy `large_enum_variant`: `ExplainResult` is ~424 bytes, so `EngineResponse::Explain` holds a `Box` (same wire shape and TS type). (3) `RowValues` needed no newtype: serde already reads/writes `Vec<(String, Value)>` as an array of `[column, value]` pairs; only a `ts(type = "Array<[string, unknown]>")` override. (4) `seaquel-rpc` was already in `INTERFACE_MAY_USE`; it gets its own "interface-glue" class (core, engine, runtime, types). (5) There were no proxy tests before; `proxy.test.ts` is the first, calling the SvelteKit handler with a stubbed `fetch`. (6) `rustfmt` on a test file also reformats `tests/common/mod.rs` via `mod common;`; reverted by hand.
- Task 11 | ~30 min | +0 Rust | −~30 TS (command-palette's duplicate CSV/JSON export → `getExportContent`); +~40 values.ts (`toNumber`, `cellText`), +~45 cell-type.ts, ~110 lines of site edits across 15 files; +~250 tests (cell-type.test.ts, export-formats.test.ts, chart-utils.test.ts, ai/context.test.ts, values.test.ts and json-export.test.ts additions) | (1) `Number(SqlDecimal)` already worked (via `toString`) and `Number(bigint)` too, so the real chart bug was `isNumeric` rejecting both (columns weren't detected as numeric) and d3 scales throwing on a bigint x value (`+10n`), fixed with `chartXValue`. (2) Added a `cellText` helper (`String()` except bytes → hex, also inside arrays; null → "") so CSV/Markdown/clipboard/FK filters/strikethrough share one rule and stay byte-identical to `String()` for ordinary values. (3) The plan's `JSON.stringify(v, null, 2)` for the edit text would put newlines into a single-line `<input>`, which drops them and makes every blur a "change", so the edit text stays compact JSON (now with `jsonReplacer`). (4) `rowsToJSON` doesn't exist; `generateJSON`/`getExportContent("json")` is the function and is tested with a bigint row. (5) `escapeSQLValue`/`generateSQL`/`getExportContent` gained an optional `dbType` for the `'\x…'::bytea` cast. (6) Svelte MCP autofixer run via its CLI (`npx @sveltejs/mcp svelte-autofixer <file>`); the remaining findings in 4 files are identical to HEAD (pre-existing unkeyed `#each`, `goto` without `resolve`, Map/Set in `$derived.by`). Review follow-up (~20 min, +~120 TS src, +~200 tests): (7) the real crash risk was persistence, not the grid. Saved workflows keep result/chart rows and were `JSON.stringify`d with no replacer, so one bigint cell aborted the whole project save (tabs, panes too); they now go through `toStorable`/`fromStorable` (wire tags; plain objects that already carry `$sq` are wrapped in a `json` tag so user JSON can't be misread). `persistProjectDashboards` skipped `stripWidgetRuntimeState` (bigint in `widget.result` threw, and the error was only logged); all three dashboard save paths now share `toPersistedDashboard` in `dashboard-serialize.ts`. No other persisted state holds rows (query tabs, AI chats, dashboard versions/files, localStorage were checked). (8) `cellText` now writes plain objects as JSON, so CSV/Markdown/SQL/clipboard no longer emit "[object Object]". (9) Inline CRUD literals: bytes are `'\xAB\x01'::BLOB` (DuckDB) and `0xAB01` (MSSQL), decimals unquoted unless NaN/Infinity.
- Task 10 | ~25 min | +0 Rust | −0 TS removed; +~590 src/lib/engine (types.ts 95, rust-engine-client.ts 199, ts-engine-client.ts 258, index.ts 39) +~790 tests (ts-engine-client 25 tests, rust-engine-client 28, index 8); http-provider.ts `postJson`/`envApiBaseUrl` pulled out of the class so the Rust client shares its error mapping | (1) Vitest runs a function returned from `beforeEach` as teardown, so `beforeEach(() => invoke.mockReset())` (mockReset returns the mock) re-invoked the rejecting mock after the test and failed it with the raw DbError; braces fix it. (2) The demo's DuckDB connection lives in the `getDuckDBProvider()` singleton, not `getProvider()`'s; `TsEngineClient` defaults to a `ProviderRegistry.getForType(type)`, which hands out the same singletons the app uses. (3) The interface takes rows as plain objects; the Rust client sends ordered `[column, value]` pairs and, for update/set-default/delete, only the primary-key pairs (the Rust builder reads nothing else), so wide rows with large bytea cells aren't shipped over IPC. Review follow-up (~10 min): both clients take `getConnectionId: () => string | undefined`, read on every call. `reconnect()` replaces the connection object in `state.connections` (spread + `map`; `disconnect` does the same with `undefined`), so capturing the object is as stale as capturing the id; `getEngineClient(connection, state?)` looks the connection up by `id` in `state.connections` when given the state, else reads the object it was handed. +4 tests (65 total) cover a client created before a simulated reconnect.
- Task 12 | ~40 min | +0 Rust | −~190 TS at the call sites (adapter+provider plumbing in connection-manager, schema-tabs, statistics-tabs, explain-tabs, query-crud, query-execution, create-table-tabs, create-table-view; `StatisticsTabManager` lost its `executeQuery` callback and `SchemaTabManager`/`ExplainTabManager` their `ProviderRegistry`), +~50 src (`latest-debounced.ts` 48, `castMapForColumns`), +~300 tests (latest-debounced.test.ts 5 tests, query-crud.svelte.test.ts 8 tests) | (1) The create-table DDL preview's `$effect` used to track the whole definition because the generator read it synchronously; once generation is async and debounced the reads happen outside the effect, so it `$state.snapshot`s the definition (and original) up front to keep deep tracking and to send plain objects over the wire. (2) The debounce + stale-reply guard is a small `latestDebounced` helper, so it is unit-testable without mounting the component. (3) `loadTableMetadataInBackground` kept its "nothing to do while disconnected" early return by checking the live `providerConnectionId` in state, since the id is no longer a parameter. (4) The demo connection's background metadata load now uses the DuckDB provider it connected with (before, it went through `getForType`, which is the default provider outside the demo). (5) E2E substitute: esbuild-bundled the real `getEngineClient`/`RustEngineClient`/`HttpProvider`/`QueryCrudManager` against `seaquel-server` + Docker Postgres; bigint PK 9007199254740993 inserted, updated and deleted exactly. Review follow-up (~10 min): TS-engine tabs load column types offline again (only Rust-engine tabs wait for a connection); the DDL preview shows a muted "Preview unavailable — reason" line (debug-logged, cleared by the next good preview) and "Connect to preview SQL" for a disconnected Postgres tab (2 new keys in messages/en.json only); removed the caller-less `generateSql`.
- Task 13 | ~15 min | +0 Rust (doc-comment touch-ups only) | −1641 TS (postgres.ts 473, recorder postgres-dialect.fixtures.test.ts 1158, scripts/fixtures/tsconfig.json 6, 2 registry lines in db/index.ts, 2 package.json lines: `fixtures:postgres` and `@types/pg`); +~10 src (lazy adapter getter in ts-engine-client.ts), +~35 tests (index.test.ts: getAdapter("postgres") throws; spy on the real `getAdapter` proves a Postgres client on desktop and web never asks for it) | `TsEngineClient` resolved its adapter in the constructor, so once `getAdapter("postgres")` throws, `getEngineClient` for a Postgres connection in the demo would have thrown at construction. The adapter is now looked up on first use, so the demo still gets a `TsEngineClient` (whose calls would fail, but the demo can't create Postgres connections). `@types/pg` was only for the recorder (`pg` itself stays: the e2e seeder uses it), and `npm install --package-lock-only` put package-lock.json back to HEAD. Frozen fixtures got a README.
- Bug fix 6 | ~40 min | +~25 Rust src (key casts in crud.rs `where_conditions`, `casts` on `build_set_default`/`build_delete` through the Dialect trait, PostgresDialect and seaquel-rpc; TEXT NULL in bind.rs) +~260 tests (smoke.rs `crud_on_typed_keys_and_columns`, 3 crud.rs unit tests, `bugfixes_crud` + 4 bugfixes.json cases, rpc dispatch casts) | +~15 TS (casts on `RustEngineClient.buildSetDefault`/`buildDelete`, `EngineClient` interface, query-crud callers), +~25 tests | (1) The live test failed first with `operator does not exist: uuid = text` and `cannot cast type jsonb to date`, as the review reported. (2) TEXT NULL still cannot go uncast into a jsonb, integer, enum or array column (`column is of type … but expression is of type text`); JSONB NULL had the same problem for everything but json. The UI casts every column except text, user-defined and array, so "Set NULL" on an enum or array column still fails. An untyped NULL (OID 0) would fix those, but `SELECT $1 IS NULL` then fails with "could not determine data type"; not done. (3) Two crud.json cases carry a cast for a key column (`id: integer`, `region: text`); the parity harness strips key columns from the cast map instead of editing the frozen fixture.
- Bug fix 6 review follow-up | ~50 min | +~220 Rust src (decode.rs: binary DATE/TIME/TIMETZ/TIMESTAMP/TIMESTAMPTZ decoders via a `temporal!` newtype macro, `civil_from_days`, OID) +~150 tests (smoke.rs `temporal_edge_values`, 17 new key cases and a 24:00-vs-00:00 check in `crud_on_typed_keys_and_columns`, 4 decode.rs unit tests) | +~15 TS (`UNBOUNDED_CAST` in query-crud.svelte.ts) +~15 tests | (1) Leaving `bit` uncast (the suggested fix) fails with `operator does not exist: bit = text`; `bit varying` works, with a length check on assignment. `char(n)` keys never matched, before phase 1 either (`bpchar = text` drops the padding on one side only); `bpchar` fixes them. (2) sqlx panics rather than erroring on ±infinity and years past 9999 (`PG_EPOCH + Duration`), so the only safe option was to stop handing it those bytes. (3) OID 0 binding is possible in sqlx 0.8 (`PgTypeInfo::with_oid(Oid(0))`), but query-editor params send empty values as NULL and `$1 IS NULL` then fails, so NULL stays TEXT. (4) The reviewer scratch program (`scratchpad/rt`) now reports 45/46 key types affecting one row; `xml` has no btree.
- Bug fix 7 | ~45 min | +~25 Rust src (`cast_type` on `SchemaColumn`, the `cast_type` subquery in `COLUMNS_SQL`, the parser) +~250 tests (smoke.rs `crud_with_catalog_cast_types`, parity SQL/parser checks, a `bugfixes.json` crud case and `castType` on its column expectations, wire_format) | +~35 TS src (async `buildCastMap` that loads missing columns, `castType` in `castMapForColumns`, `storeTableMetadata` pulled out of schema-tabs into `schema-cache.ts`) +~130 tests | (1) `format_type` verbatim would have been a regression: an explicit `CAST('toolong' AS varchar(5))` truncates silently where the old uncast assignment raised "value too long", and so do `char(n)`, `bit(n)` (zero-pads), arrays of them and domains over them. The SQL drops those lengths instead (`character varying`, `bpchar`, `"bit"`). (2) `format_type(bit, -1)` prints `"bit"` quoted, which has no length (plain `bit` is bit(1)), so bit(n) keys use their index now instead of the `bit varying` seq scan. (3) The introspection expectations in `bugfixes.json` compare columns exactly, so they gained `castType`; the frozen `parse-columns.json` rows have no `cast_type` and still parse to `None`.
- Bug fix 7 review follow-up | ~35 min | +~15 Rust src (`cast_type` now walks the domain/array chain with a recursive CTE and qualifies non-`pg_catalog` types with `quote_ident`; `CastMap` trusted-input docs) +~70 tests (smoke.rs: a two-level domain over varchar(3), an array of a domain, an enum in `public`, and `table_metadata` on a connection opened with `options=-c search_path=<scratch>,public`) | +~30 TS src (per-connection promise cache of loaded table metadata in `QueryCrudManager`, keyed by provider connection id, forgotten from the `onSchemaLoaded` callback; store only into a cached entry that still has no columns) +~60 tests | (1) The one-level `typbasetype` check let `CREATE DOMAIN v3b AS v3` (v3 over varchar(3)) through as `…v3b`, and `CAST('toolong' AS v3b)` stores `'too'`. (2) The search_path test uses the connection string's `options` parameter, so every pooled connection has it; `SHOW search_path` answers `<schema>,public` without a space. (3) All schema list loads (connect, reconnect, the connection-tab path, refresh) already go through `onSchemaLoaded`, so that's the single place to forget loaded columns.
