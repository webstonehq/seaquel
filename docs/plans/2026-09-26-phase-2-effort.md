# Phase 2 effort log (task | wall time | Rust lines added | TS lines removed | surprises)

- Task 1 | ~10 min | +0 Rust | −0 TS removed; +1 line of logic in `buildCastMap` (`connection.type !== "postgres"` → `undefined`, before any metadata load) plus doc comments; +~40 tests in query-crud.svelte.test.ts (11 cases: sqlite/mysql/mariadb/mssql/duckdb with and without the Rust engine, SQLite edits build without casts), +~95 src/lib/db/sqlite-casts.test.ts (live better-sqlite3 run of the adapter's real `buildUpdateSql`/`buildInsertSql`) | (1) The live test is a vitest over better-sqlite3 rather than the Rust driver: the Rust SQLite engine has no dialect yet (Task 9), so the TS builder is the one users hit today. It asserts both sides: no casts keeps `'2024-01-01 10:00'`, `'{"a":1}'` and untyped `'hello'` as text; the declared-type casts store 2024, 0 and a blob. (2) The adapter numbers placeholders `$1`, `$2`; better-sqlite3 binds `$N` by the name "N", so the test passes an object. (3) The check is on `connection.type`, not `usesRustEngine`, so MySQL/SQLite/MSSQL/DuckDB won't start loading metadata or casting once they move to Rust in later tasks.
- Task 3 | ~35 min (incl. review follow-ups) | +~450 Rust (testkit lib +~265/−17, tests/typed_cells.rs +~125, Postgres smoke.rs +60/−56) | −0 TS | (1) `run_typed_cells` is tested against SQLite (a testkit dev-dependency, no server needed): passing cases, a run with 4 of 5 cases failing reports all four at once, and setup tables are gone afterwards. (2) SQLite decodes a BLOB (`x'00ff'`) as `Array([Int(0), Int(255)])` today, not `Bytes`; left for Task 10, the case isn't in the testkit test. (3) Bind-back accepts only `Bool(true)` or `Int(1)` (MySQL/SQLite return 1 for `? = x`), never text/decimal/float; MSSQL cases need their own `CASE WHEN` SQL via `.bind_back(sql)`. (4) `same_value` treats float NaN as equal, so Postgres's NaN case moved into the table; its wire-format assertion stayed in smoke.rs. (5) IntrospectionExpect flags only skip checks; none assert the opposite. `supports_schemas: false` compares just the listed tables named in `tables`, for SQLite's shared `main`. Review follow-ups: `supports_statistics: false` asserts `NOT_SUPPORTED` and skips every stats check (MSSQL); `stats_has_sizes` split into `stats_has_table_sizes` and `overview_has_size` (SQLite/DuckDB have only the overview size); cases keep their `literal` so `bind_back_eq` ignores `.named()`; `run_typed_cells` documents that pooled drivers need persistent, uniquely named setup tables, never TEMPORARY ones.
- Task 2 | ~40 min incl. review follow-ups | +0 Rust | −0 TS removed; +~1,200 TS in `scripts/fixtures/` (recorder test, corpus API, replay, inputs, server/session helper, npm wrapper, tsconfig, a demo SQLite corpus) and `fixtures:record` in package.json | (1) Parse inputs are stored in the Value **wire format** (cells tagged `{"$sq":…}` where JS can't hold them exactly) and decoded with `decodeRows` at replay time, which keeps fixtures lossless (bigint/bytes/decimal would not survive `JSON.stringify` after decoding) and lets Rust read them with `Value::from_wire`. (2) `npm run fixtures:record -- <engine>` needs a tiny `record.mjs` wrapper: a positional arg passed to vitest is a file filter, and `sqlite` also matches `src/lib/db/sqlite-casts.test.ts`. (3) The demo corpus (`sqlite-demo`) writes to `$TMPDIR/seaquel-fixtures/sqlite-demo/`, not `crates/`, and is only checked with `ENGINE=sqlite-demo`; a plain `vitest run` skips (no registered corpus has fixtures yet). (4) Added a `ddl-add-column.json` group (MSSQL Task 12 needs "add column") and a `rowCount` stats kind (the per-table count TsEngineClient runs for SQLite/DuckDB, which Task 9 moves into Rust). (5) `parse-columns` inputs carry `foreignKeys` rows when the adapter has `getForeignKeysQuery` (SQLite and DuckDB do). (6) `envConnectConfig` forces the `seaquel_test` database for server engines even if the `SEAQUEL_TEST_*` JSON names another. (7) Review follow-ups: parse inputs also store `columns` (result order; `foreignKeyColumns` for FKs) because the workspace serde_json has no `preserve_order`, so Rust harnesses must rebuild QueryResult from `columns` and read cells with `Value::from_wire`; `RecordContext.openSession()` gives a fresh connection for session/global settings (MySQL EXPLAIN JSON v2); `ExplainTarget.params` binds values; per-table row counts that fail are skipped like TsEngineClient (demo proves it with a `"my-table"`); record mode deletes fixture files of undeclared groups (never bugfixes.json/README.md); header notes to record DuckDB against a temp copy of `e2e/test-databases/duckdb/seaquel_test.duckdb`.
- Task 4 | ~1 h 15 min | +0 Rust | −0 TS removed; +~780 TS corpus (`scripts/fixtures/corpora/mysql.ts`, two corpora), +~15 TS in the recorder (`stats.sql` override), 2 hand-written `bugfixes.json` (mysql 52 cases, 28 `replaces`; mariadb 17, 6 `replaces`) | (1) Recording found two more TS bugs, now fixes 9 and 10: the index usage query read `TABLE_SCHEMA` from `mysql.innodb_index_stats`, which only has `database_name`, so it failed on MySQL 8.4 **and** MariaDB 11 and took the whole Statistics view down (recorded with the fixed query through a new `stats.sql` corpus override); and MySQL's EXPLAIN JSON v2 puts the plan at the root, while the TS looked under `query_plan`, so every v2 plan was an empty `Query`. (2) Fix 4 is real on MySQL: `COLUMN_TYPE`/`COLUMN_DEFAULT` arrive as integer arrays (binary collation), so `DEFAULT 'é'` showed as `Ã©`; and `table_type` isn't aliased, MySQL names it `TABLE_TYPE`, so the TS listed every MySQL view as a table (MariaDB lower-cases it and was fine). (3) MariaDB has no `EXPLAIN ANALYZE` at all (syntax error), so its targets record `EXPLAIN FORMAT=JSON` only plus `ANALYZE FORMAT=JSON` via `parse.extra`; MariaDB JSON has `rows`/`cost` instead of `rows_examined_per_scan`/`cost_info` and wraps tables in `filesort`/`temporary_table`, so the TS showed no numbers. (4) MySQL 8.4 prints `rows=19.9`, `cost=1.84..3.06`, `42e-6`, `(never executed)`, `<temporary>` and unquoted `fx order items`/`fx_café`: 16 of the 22 recorded `EXPLAIN ANALYZE` plans and all 7 v2 plans differ from the TS by design (fixes 5 and 10). (5) The scratch objects live in `seaquel_test` itself (all `fx…`): the schema, table-size and stats queries only see `DATABASE()`. Query-block `attached_subqueries`, a `query_plan` v2 root and odd cost fields are unreachable on real servers and recorded as synthetic plans through the TS parser. (6) A MySQL CLI piped from Docker defaults to latin1 and double-encodes `é` during manual exploration; the recorder (sqlx) is fine. (7) Re-recording changes timings, so the fix 5/10 `replaces` cases must be regenerated with it (README says so).
- Task 5 | ~1 h 30 min | +~3,400 Rust (src: dialect.rs 195, introspect.rs ~1,320 after rustfmt, driver.rs +100, ddl.rs +95 incl. 2 unit tests; tests: dialect_parity.rs 490, introspect_parity.rs 775, smoke.rs +410) | −0 TS | (1) Parity: 0 of 145 MySQL and 70 MariaDB recorded cases caught a difference in the port that wasn't a planned fix; the 34 cases that differ are all fixes 4, 5, 6, 7, 8 and 10 (`replaces`, each asserted to change the recorded TS output). The only failure was a harness artifact: serde_json without `float_roundtrip` reads `0.9099999999999999` (JS `0.71 + 0.20`) as `0.91`, so EXPLAIN outputs compare within 4 ULPs. (2) `ddl.rs` gained `drop_index_on_table` (fix 6: `DROP INDEX … ON` the original table, `DROP PRIMARY KEY` for `PRIMARY`) and `modify_on_default_change` (fix 7); both default off, Postgres sets them off explicitly. (3) MariaDB is detected with `SELECT VERSION()` at connect; its EXPLAIN branch reads `rows`, `cost`, `r_rows`, `r_loops`, table times and the query/optimization times, collapses a single-operation query block, and analyzes with `ANALYZE FORMAT=JSON`, so `run_introspection` expects an execution time on MariaDB but not on MySQL. (4) Statistics degrade to empty `indexUsage` only on 1142/1044/“denied” errors, checked live with a scratch user granted `SELECT ON seaquel_test.*`. (5) smoke.rs runs the scratch objects in a fresh database per run (the queries only see `DATABASE()`), connecting to it, and drops stale ones first; the DDL fixes are executed on both servers (backticked names, `DROP INDEX … ON`, `DROP PRIMARY KEY`, a default-only `MODIFY`). (6) Not changed (follow-ups): a functional index key part still shows as `""` (MySQL `COLUMN_NAME` is NULL; MariaDB's `STATISTICS` has no `EXPRESSION` column), MySQL reports a unique NOT NULL key as `PRI` on tables without a primary key, and `Dialect::explain_sql` returns `EXPLAIN ANALYZE` for MariaDB (the driver uses its own SQL).
- Task 6 | ~1 h 35 min incl. review fixes | +~1,050 Rust (decode.rs +271/−121 incl. 5 unit tests, bind.rs +66/−12, tests/values.rs 713: 76 MySQL / 71 MariaDB typed cells, CRUD by BIGINT UNSIGNED and non-UTF-8 VARBINARY keys, DECIMAL-parameter comparison) | −0 TS removed; +6/−1 `cell-type.ts` (integral `SqlDecimal` → integer; `bit(n>1)` no longer a checkbox) +22 vitest | (1) sqlx can't tell `VARCHAR … COLLATE utf8mb4_bin` from VARBINARY: MySQL sets the BINARY flag for every `*_bin` collation (all of MySQL 8's `information_schema` names too) and sqlx drops the collation id. "Always Bytes" would show those as hex and break catalog reads, so binary strings are `Text` when they are clean UTF-8 (no control characters but tab/CR/LF), else `Bytes`; both bind back to the same bytes. (2) sqlx reports zero dates as NULL (`is_null()` is true for a zero-length DATE/DATETIME/TIMESTAMP); the decoder checks the bytes first. (3) sqlx 0.8.6's `MySqlTime::is_negative()` returns `is_positive()`; use `sign()`. (4) Before: DECIMAL decoded as `Text` (not `Decimal`) and rust_decimal rounded DECIMAL(65,30) to 28 places, or gave NULL above 28 digits; TINYINT(1) holding 5 or -1 decoded as `true` (an edit would save 1). (5) Temporal text changed to the server's format (`2024-01-02 03:04:05.5`, `01:02:03`, no `+00:00:00`): the old TIMESTAMP text made `UPDATE … WHERE ts = '…'` fail with 1292 in strict mode on both servers, and `'1:02:03.0' = TIME'01:02:03'` is false on MySQL. (6) `Decimal` binds as a NEWDECIMAL parameter carrying its exact text; MariaDB compares a plain string with DECIMAL as DOUBLE, MySQL 8.4 doesn't. (7) Zero dates can't bind back on MySQL (default NO_ZERO_DATE turns `'0000-00-00'` into NULL); `INSERT IGNORE` stores them there, a `SET_VAR(sql_mode='')` hint doesn't. (8) Another agent was editing introspect.rs/smoke.rs during the final check: `mysql_window_explain` and `{mysql,mariadb}_defaults_round_trip` (new, theirs) failed; every other test passed, and all smoke tests passed with the new decoder before their edits. Review fixes: (9) TIME is read from the binary-protocol bytes (sign, days, h, m, s, µs) like dates: sqlx's `MySqlTime::new` rejects MariaDB's `±838:59:59.999999` (MySQL's range stops at `838:59:59.000000`); 6 more live TIME cases per server. (10) Only text types fall back to `Bytes` (invalid UTF-8); a number or date that fails to decode is a `QUERY_ERROR` ("can't decode a TIME value: …"), and `impl_sqlx_driver!` now appends the column name to any decode error (`seaquel-engine` `__private::in_column`, all sqlx engines). (11) Dropped the unused `time` dependency. (12) Documented that TIMESTAMP is UTC wall-clock (sqlx sets `time_zone='+00:00'`) and why `?timezone=` must not be added (DST folds).
- Tasks 4–5 review follow-ups | ~50 min | +~420 Rust (introspect.rs defaults/window/per-loop, ddl.rs option rename, driver error codes, smoke.rs +~200, parity tests +~150) | −0 TS; corpus restores the `explain_json_format_version` it read | (1) MariaDB's `r_*_time_ms` are cumulative over `r_loops`; now divided (tables, filesort, query blocks), checked on the recorded `ANALYZE FORMAT=JSON: join` (`c` has 333 loops). (2) The MariaDB walker handles `window_functions_computation` (`sorts[]` + `temporary_table`) and `having_condition` (a `Having` node), and walks into any unknown object/array, so new wrappers keep their tables; live window/HAVING EXPLAIN on both servers. MySQL's v1 JSON had the same gap (`windowing`, the whole plan dropped): added to fix 10 with a hand-written case. (3) Defaults are SQL expressions now (fix 8 reworked, `EXTRA` added to the columns query): the recorded MySQL rows have no `EXTRA`, so they keep TS values and the new form is covered by a hand-written `parse-columns` case and live tests; MariaDB's replacements shrank from 6 to 5 (only its bare `NULL` changes). (4) Fix 7 is `SET DEFAULT`/`DROP DEFAULT` (`ddl.rs` `alter_default_with_modify_column`); live round trip on both servers: the comment survives, an introspected table re-created from its own definition has the same defaults. Surprise: MariaDB 11 drops a TIMESTAMP's `ON UPDATE` on `ALTER COLUMN … SET DEFAULT` (its own `SHOW CREATE TABLE` loses it); MySQL keeps it; the test pins both. (5) `MysqlDialect::explain_sql` is documented MySQL-only and delegates to `introspect::explain_sql(…, Flavor::Mysql)`; only drivers run EXPLAIN. The permission check parses sqlx's `number (SQLSTATE)` and matches 1142/1143/1044 with 42000. Parity: still 0 cases caught a port bug.
- Task 7 | ~45 min | +0 Rust | −0 TS removed (mysql.ts goes in Task 19); +~260 TS logic (`RUST_ENGINES`/`RustEngine`/`PAGINATE` + mysql/mariadb, MySQL branch in `query-params.ts` +132, `binaryCellBytes`/`binaryStringEncoding`/`editedCellValue` in cell-type.ts, `fromHex` in values.ts, a `binaryStrings` prop through data-viewer → virtual-results-table → editable-cell → formatted-cell), +~210 vitest (query-params.test.ts new, MySQL paginate parity for both types, binary display/edit, fromHex), CLAUDE.md note; e2e script in the session scratchpad (`e2e-mysql/`) | (1) The e2e (esbuild-bundled real `RustEngineClient`, `QueryCrudManager`, `substituteParameters` and cell helpers against `seaquel-server` + Docker) passed first time on MySQL 8.4 and MariaDB 11: schema with a spaced table name, metadata, stats, EXPLAIN/ANALYZE with bound `?`, BIGINT UNSIGNED 18446744073709551615 key, DECIMAL(65,30) round trip, VARBINARY keys as Bytes (`00ff10`) and Text (`abc`), a `\x00ff41` edit stored as 3 bytes, default-only alter → `ALTER COLUMN … SET DEFAULT`; Postgres bytea edit too. (2) MySQL params: `?` is positional, so a parameter used twice is bound twice; a literal holding `{{p}}` becomes one `?` bound to the filled-in text (NULL if a value is NULL, like CONCAT), with MySQL's backslash escapes resolved (`\%`/`\_` kept); comments are left alone so a `?` there can't misalign binds. (3) MSSQL still sends binary cells as base64 strings (TS dialect, Rust driver's JSON fallback), so "string in a binary column = its UTF-8 bytes" would have broken MSSQL's display: the data viewer picks the encoding per connection type (`base64` for MSSQL, `utf8` otherwise), and MSSQL cells now also show hex + exact size instead of an estimate. (4) `tinyblob`/`mediumblob`/`longblob` weren't classified as binary; now they are. (5) Pre-existing, parity with TS, not changed: MySQL `EXPLAIN ANALYZE` names a filter node `Filter:` (trailing colon, condition dropped). (6) During the final check `scripts/fixtures/dialect-fixtures.test.ts` failed on the SQLite corpus (`bugfixes.json covers fix 1`), from another agent's in-progress Task 8 files; not touched.
- Task 8 | ~40 min | +0 Rust | −0 TS removed; +~690 TS corpus (`scripts/fixtures/corpora/sqlite.ts`, registered in `corpora/index.ts`; recorder core untouched), hand-written `bugfixes.json` (56 cases, 36 `replaces`) + README | (1) 161 recorded cases (quote 8, paginate 4, crud 12, ddl-create 10, ddl-alter 15, ddl-add-column 3, column-types 1, sql 13, schema 1, columns 7, indexes 7, explain 68 incl. 8 synthetic, stats 12) from a fresh temp-file database; `SEAQUEL_TEST_SQLITE` can override it. (2) Recording found two more TS bugs, now fixes 9 and 10: a foreign key declared without parent columns (`REFERENCES customers`, common in SQLite) has a NULL `to`, which the TS showed as `referencedColumn: null`; and `NOT LIKE 'sqlite_%'` treats `_` as a wildcard, so a user table named `sqlitex` or `SQLiteData` was missing from the schema tree and statistics. (3) SQLite 3.46 (bundled with sqlx) prints details the TS regexes never matched: 23 of 60 real plans (and 1 synthetic) fell back to a first-word chip (`SCALAR SUBQUERY 1` → "SCALAR", `LIST SUBQUERY 1`, `BLOOM FILTER ON c (score=?)`, `… LEFT-JOIN`, `RIGHT-JOIN b`, `SCAN CONSTANT ROW`, `USE TEMP B-TREE FOR LAST 2 TERMS OF ORDER BY`/`count(DISTINCT)`, `CO-ROUTINE (subquery-2)`, virtual tables, and every name with a space, `-` or non-ASCII letter; an FK-cascading `DELETE` plans a `SEARCH order items USING COVERING INDEX sqlite_autoindex_order items_1`). Modern SQLite prints aliases only (`SCAN c`), never `t AS a`; the old forms are synthetic cases. (4) Names that fail `validateIdentifier` (`my-table`, `order items`, `it's`) can't go through the adapter's PRAGMA queries at all, so they are hand-written live cases (fix 1). (5) Not a listed fix, left as a follow-up: `buildSetDefaultSql` emits `UPDATE … SET "c" = DEFAULT`, which SQLite rejects (it has no `DEFAULT` in UPDATE), and the dialect has no way to know the column's default expression.
- Task 9 | ~1 h 20 min | +~2,900 Rust (src: introspect.rs 899 incl. 5 unit tests, dialect.rs 247, timing.rs 16, driver.rs +90, lib.rs +8; seaquel-engine ddl.rs +~110 incl. 1 test; tests: dialect_parity.rs 509, introspect_parity.rs 431, smoke.rs 547; rpc/server tests +~100) | −0 TS | (1) Parity: 0 of 161 recorded cases caught a port difference that wasn't a planned fix; the 36 that differ are fixes 2 (1), 3/4/6 (7), 7 (3), 8 (24) and 9 (1), each a `replaces` asserted to change the TS output; the first run of the harness showed exactly those. (2) `ddl.rs` gained `qualify_index_name` (fix 3: `CREATE INDEX "main"."i" ON "t"`), `supports_add_foreign_key` and `unsupported_notes: Option<&str>` (fixes 4 and 6: comment lines). The notes go after every statement: the table editor splits on `;\n` and drops pieces starting with `--`, so a note between statements would have swallowed the next one; line breaks in names become spaces so a name can't end the comment. Postgres/MySQL set the new options off. (3) sqlx's SQLite binder reads `$N` as the Nth argument, so CRUD keeps the TS `$1…` text; the cast map is ignored (Task 1). (4) Fixes 1/5/9 use table-valued functions with bound names (`pragma_table_info(?)`, `pragma_index_list(?) LEFT JOIN pragma_index_info(il.name)`, `pragma_foreign_key_list(?)` with a correlated `pragma_table_info(fk."table")` for the implicit parent column); an expression key is `<expression>`. (5) `formatBytes` needed a JS `toFixed` port: Rust's `{:.2}` rounds ties to even (1.125 KB → "1.12", JS "1.13"), and ties are realistic (page_count ≡ 32 mod 64 at 4 KiB pages gives x.xx5 MB). (6) Row counts and EXPLAIN ANALYZE timing moved from TsEngineClient; the clock is `std::time::Instant` behind `#[allow(clippy::disallowed_types)]` in `timing.rs` (engine crates are native-only). ANALYZE executes the statement (writes included), documented and tested. (7) `seaquel-rpc`'s dispatch test and `seaquel-server`'s 501 test used SQLite as "the engine without a Rust dialect"; they now use a bare `duckdb` mock, and a new server test checks SQLite answers `listSchemas` = `["main"]`. (8) `run_introspection` flags as the plan says; `stats_has_index_usage` stays on (the index list has `N/A` sizes). Follow-ups: `SET … = DEFAULT` (see Task 8); generated columns are missing because `pragma_table_info` hides them (`pragma_table_xinfo` has them); when every edit is a comment line the editor runs nothing yet reports success (Task 10 should warn); a WITHOUT ROWID table's primary-key index is in `table_metadata` but not in the statistics' index list (not in `sqlite_master`).
- Tasks 8–9 review follow-ups | ~25 min | +~200 Rust (ddl.rs options `inline_foreign_keys_on_added_columns` and `constraint_index_prefix` + 1 test, 2 hand-written bugfix cases, 1 live test) | −0 TS | (1) Removing a `sqlite_autoindex_*` from the editor's definition was `DROP INDEX`, which SQLite rejects; it's now a note. No origin field was needed: SQLite reserves the `sqlite_` prefix, so the name alone identifies a constraint's index. (2) A foreign key on a column added in the same edit goes inline (`ADD COLUMN … REFERENCES "t" ("id")`, unqualified, because SQLite's REFERENCES takes no schema), but only when the column has no default. (3) The "nodialect" rename wasn't possible: `ConnectConfig.driver` is the closed `DriverType` enum, so a made-up id can't connect. The mock keeps the "duckdb" id, is renamed `NoDialectEngine`, and is documented; neither test Core registers the real DuckDB engine, so Task 17 doesn't affect them.
- Task 11 (incl. two review rounds) | ~1 h 50 min | +~1,500 Rust (session.rs ~280 new, driver.rs +~80 net, lib.rs +8, live_tests.rs ~120 new; tests: live.rs ~480 new, row_cap.rs ~60 new, common/mod.rs ~75 new, smoke.rs +6) | −0 TS | (1) Cells are read by position (`Row::cells()` + `FromSql` on each `ColumnData`, same type order as before), so `SELECT a.id, b.id` and `SELECT 1, 2` keep their own values; columns come from each result set's metadata, so an empty result still has them (`query_stream` too, via the default). (2) `transaction`: `IF @@TRANCOUNT > 0 THROW 50000 …; BEGIN TRANSACTION` and `COMMIT TRANSACTION` as plain batches (a transaction begun inside `sp_executesql` must end there, error 266), each statement through `sp_executesql`, the client held throughout. It refuses to nest in a hand-opened transaction (`EXECUTE_ERROR`, that transaction untouched). First failure → `IF @@TRANCOUNT > 0 ROLLBACK TRANSACTION` and the statement's `EXECUTE_ERROR` (bind errors: `QUERY_ERROR`, before BEGIN), like the sqlx macro; the `@@TRANCOUNT` guard covers XACT_ABORT ON and statements that end the transaction themselves; the session's XACT_ABORT is left alone. (3) Review C1: a caller dropped after BEGIN is sent but before its reply is read left tiberius's transaction descriptor at 0 while the server had a transaction open, and every later request failed with 3989. Fix: poison and reconnect. The mutex holds `{ client, dirty }`; `dirty` is set before any I/O and cleared only once the response has been read to its end (in a transaction: after COMMIT or a successful rollback). A dirty connection is dropped on the next lock (the server rolls back and aborts the running statement) and replaced with a fresh one from the kept `ConnectConfig`; a failed reconnect leaves it dirty for the next call. RESULT_TOO_LARGE, a panic, a non-server error, 3988/3989 and a failed rollback leave it dirty too. That also fixes a half-sent request (8 MB parameter, one poll) and M2: a dropped `WAITFOR DELAY '00:00:05'` no longer blocks the next caller, whose query takes ~20 ms (reconnect) instead of ~5 s; a transaction dropped mid-`WAITFOR` holding a row lock → next query ~40 ms, no row. Live drop tests: the BEGIN window (`now_or_never`), 10 timeouts from 0 to 50 ms (all or nothing: dropped after COMMIT is sent it may have committed, all three rows), mid-statement, mid-send (query and transaction), long WAITFOR. Side effect: a reconnect loses session state, including `#temp`/`##temp` tables and a hand-opened transaction (the reviewer's `##probe_t11` probe table vanishes after its drop probes). (4) Multiple result sets: `MssqlDriver::query_results(sql, params)` returns every set (`ResultSet { columns, rows }`); `query` keeps the first, reads and drops the rest (not counted against the cap), and fails if a trailing set errors (tiberius reports a server error only after the response is read to its end). tiberius's `into_results` loses empty trailing sets and their columns, so it isn't used. The row cap counts rows as they arrive (tested in its own binary, `row_cap.rs`, since the cap is read once per process). Public surface: `MssqlDriver` (`connect`, `query_results`, `Driver`) and `ResultSet`; `Session` (`driver.session()`, `query(sql, &params)`, `batch(sql)`, `begin_transaction`/`end_transaction`) is `pub(crate)` and tested in `src/live_tests.rs`. Found for Task 13: under `SET SHOWPLAN_XML ON` a `sp_executesql` call returns no result set at all, only a batch returns the plan, so estimated plans can't bind parameters; `SET STATISTICS XML ON` works through `sp_executesql` with parameters and adds the plan as the next result set (none for trivial plans such as `SELECT 1`). Live tests now run in `seaquel_test`, and cleanup is best effort. (5) Second review: `Session`'s `Drop` closes a dirty connection at once (a tiberius client has no `Drop`; dropping it closes the socket synchronously, without TLS close_notify, so it can't block), so a dropped transaction's locks are freed without waiting for the driver's next call (live test: another connection reads the locked row under `LOCK_TIMEOUT 1000`; it fails with 1222 without the `Drop`). `hold_state`/`release_state` replace begin/end_transaction for any session state (transactions, SHOWPLAN): unreleased, the connection is closed. `Client::connect` (TLS + login) has a 30 s timeout; server errors of class 20+ poison. The mid-send test can no longer pass vacuously: it polls by hand and drops only once `sys.dm_exec_connections.num_reads` has grown while the UPDATE it carries hasn't run (NOLOCK read). `SmokeSpec::AT_P` now has `supports_transactions: true` (only MSSQL uses it).
- Task 10 | ~1 h 15 min | +~800 Rust (decode.rs 82 (was 85) incl. 1 unit test, tests/values.rs 318: 37 typed cells + CRUD by BLOB, BLOB-in-TEXT and 2^53+1 keys; `Dialect::build_set_default_expr` + `crud::build_param_set_expr` +~60, SQLite override +~30, rpc `column_default` +~10; ddl.rs +~50 incl. 1 test; smoke.rs +~95; dialect_parity.rs +~50; 7 hand-written fix 11 cases) | −0 TS removed; +~70 TS logic (`sqlite` in `RUST_ENGINES`/`RustEngine`/`PAGINATE`, `columnDefault` through `buildSetDefault`, `setDefaultExpression` in query-crud, `splitDdlScript` + note toasts in `executeCreate`, 2 en.json keys), +~220 vitest (SQLite paginate parity, create-table-tabs.svelte.test.ts new, ddl-script.test.ts new, set-default tests); e2e in the session scratchpad (`e2e-sqlite/`) | (1) sqlx's `SqliteValueRef::type_info()` is already the storage class (`sqlite3_value_type`; the declared type only for NULL), so the old decoder's DATE/BOOLEAN/… branches were dead; the new one has INTEGER/REAL/TEXT/BLOB only. Infinity is a REAL and now reaches the UI (the old JSON path turned it into NULL); NaN can't exist in SQLite. (2) TEXT that isn't UTF-8 (`CAST(x'ff' AS TEXT)`) was NULL; it is now lossy text, not Bytes: SQLite tells TEXT from BLOB, so a key shown as bytes would bind as a BLOB and could update a BLOB row with the same bytes (the e2e's first draft hit exactly that). (3) Fix 11 (Set to default): a new `Dialect::build_set_default_expr` with a default that delegates to `build_set_default`, so Postgres/MySQL and their parity tests are untouched; SQLite emits `SET "c" = (<dflt_value>)`, `(NULL)` without a default. The UI reads the default from fresh `tableMetadata` (a stale cache would apply an old default) and fails if the column is missing rather than writing NULL. Without the expression SQLite keeps the TS `= DEFAULT`. (4) The tutorial doesn't touch EngineClient (provider connect/execute/select only), so the switch doesn't reach it; `sqlite::memory:` is one shared-cache database across the pool (e2e: 12 concurrent selects). Not verified, pre-existing: sqlx's pool closes idle connections after 10 min (min 0), which would drop that in-memory database. (5) TsEngineClient's SQLite ANALYZE timing is left: TsEngineClient still serves SQLite in the demo, which has no SQLite provider, so it's dead in practice; Task 19 removes it with the adapters. (6) A NULL cell in a BLOB column still saves `\x…` input as text (SQLite's binary encoding is `text`); only a cell that holds bytes saves hex as bytes.
- Task 10 review follow-ups | ~25 min | +~260 Rust (dialect.rs `default_operand` +~70 incl. a 27-case unit test, driver.rs `pool_options` +~60 incl. 1 test, ddl.rs +~15, smoke.rs +~85, 10 hand-written fix 11 cases) | −0 TS | (1) SQLite's DEFAULT clause stores a bare word or quoted name (`DEFAULT active`, `"dq"`, `` `bt` ``, `[br]`) as a string, and `dflt_value` keeps the raw token, so `SET s = (active)` copied the `active` column; those now become string literals, keywords (`TRUE`, `NULL`, `CURRENT_*`), signed/hex numbers, blob literals and `(expr)` (shown without its parentheses) stay as they are. The live test compares Set to default with what an INSERT stores, next to decoy columns holding 'SECRET'. (2) The autoindex-rename skip needs the added index to be unique too; a non-unique index on the same columns is created. The note now says "can't drop or rename index … recreate the table to change it". (3) In-memory SQLite pools keep one connection for good (`min_connections(1)`, no idle timeout, no max lifetime); the pool options are unit-tested. A live simulation of the reaper wasn't cheap: closing an acquired connection closes the pool's only one, which isn't what the reaper does.
- Task 15 | ~1 h | +~780 Rust (driver.rs 453 (was 264), blocking.rs 149 new, tests/live.rs 278 new: 7 live tests) | −0 TS | (1) Every DuckDB call (connect, query, query_stream, execute, transaction) runs in `spawn_blocking` on an `Arc<Mutex<Connection>>`; `JoinError` maps to the op's error code. (2) The only duckdb-rs panic DuckDB 1.5 actually reaches is `unreachable!("invalid value: Time64(ns)")` for a non-NULL `TIME_NS`; `INTERVAL 1 YEAR` and the other interval kinds arrive as MonthDayNano and read fine. Each cell read is under `catch_unwind` and a panic becomes `UNSUPPORTED_TYPE` naming the column; an up-front type check would have broken NULL `TIME_NS`, which reads fine today. The worker also catches panics while it holds the mutex, and poison is recovered anyway. (3) `query_stream` is a blocking producer feeding a 2-batch channel. Dropping a call's future (or stream) calls `duckdb_interrupt` only while that call holds the connection, and the worker checks a flag per row; measured ~0.5–1 ms from drop/cancel to the connection serving the next query (`SELECT count(*) FROM range(1e10)`), transactions roll back. (4) Cell output byte-identical to before for every type that worked (diffed a 10-query, ~50-type baseline).
- Task 12 | ~1 h 20 min | +0 Rust | −0 TS removed; +~585 TS corpus (`scripts/fixtures/corpora/mssql.ts`, registered in `corpora/index.ts`), +~190 `scripts/fixtures/mssql-showplan.ts` (ShowPlan capture with the Node `mssql` package, wired into the corpus's `parse.extra`, so `npm run fixtures:record -- mssql` is still one command), +37 `scripts/fixtures/mssql.d.ts` (types for `mssql`), `jsdom` + `@types/jsdom` dev dependencies; recorder core untouched; hand-written `bugfixes.json` (53 cases, 27 `replaces`) + README | (1) 130 recorded cases (quote 10, paginate 14, ddl-create 8, ddl-alter 19, ddl-add-column 4, column-types 1, sql 8, schema 1, columns 8, indexes 8, explain 49 incl. 5 synthetic); a second record run is byte-identical. No CRUD group (decision 4: the Rust CRUD binds `@P{n}`). (2) The TS EXPLAIN is broken end to end: SQL Server rejects `SET SHOWPLAN_XML ON` in a batch with the query, and the STATISTICS XML plan comes after the query's rows. Plans are captured with Node `mssql` on one pooled connection, each SET its own batch. The plan is `recordsets[1]` for a SELECT but `recordsets[0]` for DML (no rows first), and there's none for `SELECT 1`, so the Rust port should pick the result set by its showplan column. Run-dependent XML (compile/run times, memory grants, cache flags, `_WA_Sys_*` object ids, the `StatisticsInfo` order, `LastUpdate`) is normalized. The parallel plan's per-thread split varies, so it's recorded without ANALYZE. (3) `parseExplainResult` needs `DOMParser`, which vitest's Node environment lacks: the corpus installs jsdom's on `globalThis`. (4) The Rust driver sends everything through `sp_executesql` with a parameter list, and there `CREATE SCHEMA/VIEW/PROCEDURE` is a syntax error ("Incorrect syntax near the keyword …"). The scratch setup wraps them in `EXEC (N'…')`; for users it's a driver bug. (5) Fix choices: fix 4 takes the FK whose name sorts first (as MySQL does); fix 7's default change is one `;\n`-free piece, `DECLARE @dfN nvarchar(max) = (SELECT N'ALTER TABLE … DROP CONSTRAINT ' + QUOTENAME(d.name) …); IF @dfN IS NOT NULL EXEC (@dfN); ALTER TABLE … ADD DEFAULT … FOR [c];`. The plan's `EXEC('…' + QUOTENAME(@n))` isn't valid, since EXEC() takes no function calls. Every fix 7 output ran against real tables (in rolled-back transactions) and every fix 8 output parses under `SET PARSEONLY ON`. The only failures were SQL Server's dependency errors (4922) on columns that an index, UNIQUE, FK or (for DROP COLUMN) a default uses. (6) Found beyond fixes 1–9, not in `bugfixes.json`: `stripBrackets` leaves `]]` in plan relation and index names; column types lose length and precision (`nvarchar`), so an editor ALTER COLUMN makes an `nvarchar(100)` an `nvarchar(1)`; DROP INDEX on a PK or UNIQUE constraint's index is refused; indexes are dropped after column alters, which they block; DROP COLUMN fails on a column with a default; a trailing `;` or `--` comment breaks pagination.
- Task 15 review follow-ups | ~30 min | +~110 Rust (driver.rs `prepare` + checks, blocking.rs doc, live.rs +1 test, core comment) | −0 TS | (1) DuckDB clears its interrupt flag at every prepare and execute, so a cancel that landed between the two was lost. Every prepare (query, stream, execute, each transaction statement) is now followed by a cancelled check, and a cancelled transaction rolls back instead of committing. (2) A live test drops a query while DuckDB is still binding it (a `read_csv` sniff of the whole file, sample_size = -1, ~2.9 s in debug, before an endless execution). Connection free when the bind ends. (Corrected in the next line: that test doesn't catch a missing check.) (3) The waiting-call test now scans a range (`sum(i % 7)`) and asserts it runs > 500 ms.
- Task 12 follow-up (fixes 10–14) | ~35 min | +0 Rust | −0 TS; `bugfixes.json` 53 → 81 cases (39 `replaces`), a `dbo.fx_types` scratch table, the plan's Task 12 fix list, a Task 13 driver bullet and a follow-up | (1) The expected ALTER output now comes from a model of the fixed generator (in the session scratchpad), not hand edits: every DROP INDEX becomes fix 12's `IF EXISTS (… sys.key_constraints …) … DROP CONSTRAINT … ELSE DROP INDEX … ON …`, and dropped indexes come first (fix 13). Every expected ALTER ran against real tables in rolled-back transactions. The fix 10 nullability-only edits keep `nvarchar(400)`, `nvarchar(max)`, `decimal(12,2)` and `time(0)` at their sizes, where the bare type made `nvarchar(1)`. What still fails live is a column that a UNIQUE constraint or a foreign key uses (4922). (2) Fix 10's `parse-columns` `replaces` cases carry the fixed query's rows as `input`, as MySQL's fix 4 did, because the recorded rows have no sizes. (3) Fix 14 drops everything after the last token (whitespace, `;` and comments), and all 12 fix 8 and 14 outputs parse under `SET PARSEONLY ON`.
- Task 15 second review follow-ups | ~40 min | +~120 Rust (live.rs `run` helper, bounded waits, relative bind timing, TempDir guard; driver.rs +1 unit test) | −0 TS; ci.yml `timeout-minutes: 45` on the rust and engines jobs | (1) The earlier "the bind test hangs without the check" came from a macOS shell without `timeout`; it was wrong. An interrupt during the bind makes DuckDB fail the prepare itself, so the bind test passes with or without the check. What the check catches is an interrupt that lands before execute: the new unit test `cancel_that_lands_before_execute_is_not_lost` drops the call inside the worker before prepare. It fails without the check (the query ran 24.8 s in debug, 2.9 s in release) and passes with it in ~ms. (2) Live tests run through `run()`: a hand-built current-thread runtime, `catch_unwind`, `shutdown_timeout(1 s)`, re-raise. Post-cancel waits time out after 20 s. `ENDLESS` is now 10^12 rows (10^10 finished in ~8 s in debug). With the interrupt disabled, 3 tests fail in 21 s instead of hanging. (3) The bind test drops at bind/2 with a 100 ms floor (bind ~2.85 s debug, ~0.38 s release). The waiting-call test scans 200M rows (~3.2 s debug, ~0.41 s release; threshold 250 ms).
- Task 12 review follow-ups (fix 15 and the review items) | ~40 min | +0 Rust | −0 TS; `bugfixes.json` 81 → 103 cases (48 `replaces`), `docs/plans/artifacts/2026-09-27-mssql-alter-model.py.txt` (the case generator, including the ALTER and pagination models) | (1) The pagination expectations now come from a small tokenizer model (nested `/* */`, `]]`, `""`, `''`, `--`), and it reproduces every recorded paginate output the fixes leave alone. (2) Fix 15, collation, needs a new optional `collation` field on `SchemaColumn` and `CreateTableColumn` (Task 13). Live, ALTER COLUMN without COLLATE reset `Latin1_General_BIN` to the database default, and with it the collation stayed. (3) `sql.json` is superseded by `sql` cases (the fixed catalog queries' exact text) and `explain-batches` cases.
- Task 17 (fixtures) | ~1 h 30 min | +0 Rust | −0 TS removed; +~740 TS corpus (`scripts/fixtures/corpora/duckdb.ts`, registered in `corpora/index.ts`; recorder core untouched), generated `bugfixes.json` (51 cases, 27 `replaces`) + README, generator copy in `docs/plans/artifacts/2026-09-27-duckdb-bugfixes-model.ts.txt` | (1) 146 recorded cases from native DuckDB 1.5.5 on a temp copy of `seaquel_test.duckdb` (quote 8, paginate 5, ddl-create 9, ddl-alter 19, ddl-add-column 5, column-types 1, sql 13, schema 1, columns 9, indexes 3, explain 63 incl. 11 analyzed and 18 synthetic, stats 10); a second record run is byte-identical and the original file's checksum is unchanged. Analyzed plans are normalized (timings, CPU, memory, sorted keys). No CRUD group (decision 4). (2) No recorded parse input depends on the Task 16 decoding: every catalog cell is VARCHAR, BOOLEAN or a BIGINT count. The trap is for the port: `constraint_column_names` and other LIST/MAP catalog columns arrive today as a debug dump of the whole column chunk, and `sum()` is HUGEINT; the fixed queries unnest or cast them. (3) Fix 6 is worse than duplicated schemas: an attached catalog's namesake table merges its columns into the other's, and when both have a primary key the adapter's scalar subquery returns two rows and DuckDB 1.3+ fails the whole columns query. Decision: leave out `system`/`temp`, list the default catalog's schemas as they are, and every other catalog's as `catalog.schema`, matched by the same expression in each query; building `"catalog"."schema"."table"` in the data tab, CRUD and DDL is a follow-up. (4) Fix 2: `duckdb_indexes().expressions` is VARCHAR with inconsistent quoting; casting it to `VARCHAR[]` and unnesting it with ordinality gives one clean row per key. PRIMARY KEY/UNIQUE constraints have no row there. (5) DuckDB-WASM 1.4.3 (the demo) vs native 1.5.5: 29 parse cases equal, 39 differ, all from the plan `Table` (`seaquel_test.main.orders` vs `orders`), plan shapes, and `AS "DATE"` vs `AS DATE` in defaults. (6) Found beyond fixes 1–7, not in `bugfixes.json`: unqualified DROP INDEX fails outside `main`; DuckDB rejects `ADD COLUMN … NOT NULL`; column alters on an indexed table fail, and DROP INDEX comes after them; CREATE TABLE writes `REFERENCES ""."t"` for an FK without a schema; bare ARRAY/LIST/MAP/STRUCT/UNION column types; FILTER's `Expression` is ignored; `relationName` carries the catalog in 1.5; the overview name is a constant.
- Task 13 | ~1 h 30 min | +~3,000 Rust (seaquel-engine introspect.rs 246 new, the shared row/number/node helpers, with −~330 duplicated lines across the Postgres, MySQL and SQLite introspect.rs; ddl.rs +~170 for `drop_indexes_first` and `tsql`; mssql dialect.rs 434, introspect.rs 435, driver.rs +~210 (`Request`, `must_start_batch`, introspection), session.rs +~40 (`Keep::Plans`); tests: dialect_parity.rs 427, introspect_parity.rs 315, smoke.rs 851, live.rs +~145; seaquel-types `collation` +~10) | −0 TS removed; +~50 TS (`addFromTable` copies `collation`, 1 vitest) | (1) Dedup: the three `number_or_zero` copies differed only on text cells: MySQL's read them as JS `Number(string)` (hex/octal/binary prefixes, JS whitespace), Postgres and SQLite with Rust's `parse`. The shared one is MySQL's; both agree on every catalog value those engines return and every parity case. `Row` is shared with `get/has/str/int/number`; each engine keeps its own text readings as a local extension trait. (2) Porting the Python model in `docs/plans/artifacts` line by line, the generators and parsers matched all 54 `replaces` and 55 hand-written cases (18 of them live) on the first run. (3) Parameterless statements go as a plain batch, but tiberius only reports affected rows for RPC calls, so `execute` stays `sp_executesql` unless the statement must start a batch (CREATE/ALTER VIEW, PROC, FUNCTION, TRIGGER; CREATE SCHEMA, DEFAULT, RULE); the `EXEC (N'…')` workarounds are gone from the scratch setup. A `SET` in a parameterless query now lasts for the session. (4) A plain EXPLAIN with parameters runs `DECLARE @P1 <type>, …;` + the query under SHOWPLAN_XML: no value is interpolated, and the plan compiles. ANALYZE keeps only the showplan result set (`Keep::Plans`), so a big SELECT's own rows don't hit the row cap. (5) Base64 text used as a varbinary key converts silently and matches no row (0 affected), not an error: Task 14 must send bytes. datetime2(7) keys read back with nine fractional digits still convert exactly.
- Task 17 (fixtures) follow-up (fixes 8–15) | ~45 min | +0 Rust | −0 TS; `bugfixes.json` 51 → 109 cases (79 `replaces`), the corpus's "drop an index in another schema" now starts in `sales`, plan and README updated | (1) The generator's DDL and EXPLAIN models now switch each fix on and off: with all off they must reproduce every recorded TypeScript output (they do), and a case's fix numbers are the fixes whose removal changes it, so every changed recorded case is replaced exactly once (fix 14's `relationName` alone touches 37 plans). (2) DDL is checked the way the table editor runs it, statement by statement in autocommit on a fresh database: inside a transaction DuckDB keeps a dropped index's dependencies, which hid fix 10. (3) DuckDB refuses a rename, type or NOT NULL change on a table with any CREATE INDEX index, even on another column (ADD COLUMN and defaults are fine; PK/UNIQUE constraints don't block). Decision: don't drop and recreate kept indexes (expression keys can't be rebuilt from the definition), add a note naming them. (4) Still failing live and left alone: DuckDB won't drop or retype a column in a UNIQUE/PK constraint. (5) Fixes 9 and 10's notes need new `AlterTableOptions` switches in the port; `ddl.rs` wasn't touched.
- Task 17 (fixtures) review rework | ~50 min | +0 Rust | −0 TS; `bugfixes.json` 109 → 116 cases (79 `replaces`), 7 scratch objects with dotted/quoted schema and catalog names, WASM check copied to `docs/plans/artifacts/2026-09-27-duckdb-wasm-check.ts.txt` | (1) Fix 10 now follows the other engines: while kept indexes remain, each statement DuckDB rejects (RENAME/DROP COLUMN, TYPE, SET/DROP NOT NULL) is a note carrying it, and so are later statements on a column whose rename became a note (its default, a new index on it); every ALTER output now runs to the end live. Testing found DuckDB also refuses DROP COLUMN and TYPE on any PRIMARY KEY/UNIQUE column, which the definition's flags show, so those are notes too. (2) Fix 6 listing quotes a catalog or schema part holding `.` or `"` (`"fx.a.b"`, `"fx.we""ird".main`), so a default schema literally named `fx_aux.main` lists as `"fx_aux.main"` and can't collide; DDL splits it quote-aware (fix 14's parser), checked live in an attached catalog. (3) Fix 11: an FK in an attached catalog's CREATE TABLE is a note (DuckDB: "FOREIGN KEY constraints cannot be defined cross-database"). (4) Fix 13 is FILTER nodes only.
- Task 13 review follow-ups | ~50 min | +~400 Rust (driver.rs batch policy, `mentions_use`/`restore_database` + 2 unit tests; ddl.rs `collate` inline sizes + 1 test, COLLATE on ADD; dialect.rs COLLATE on CREATE/ADD; tests: live.rs policy, `#temp` and cleanup, smoke.rs `cleanup_after`, stale-prefix cleanup, fix 15 CREATE/ADD live; common `drop_stale`) | −0 TS; `bugfixes.json` +3 fix 15 cases (112 cases, 54 `replaces`), columns query compares `COLLATE DATABASE_DEFAULT` | (1) Policy (a): everything goes through `sp_executesql` except parameterless statements that must start a batch, so the Task 13 note "a `SET` in a parameterless query now lasts for the session" no longer holds. (2) `USE` is not scoped to an RPC call: `USE master` through `sp_executesql` left the connection in master (the live test caught it). The driver now runs `USE [<connection's database>]` after any statement with the keyword `USE` outside strings and comments, and leaves the connection dirty (reconnect) if that fails. (3) A hand-typed `BEGIN TRANSACTION` through `query` is back to reporting error 266 while taking effect, as before Task 13. (4) With `COLLATE DATABASE_DEFAULT` on both sides, the live columns output is unchanged.
- Task 17 (fixtures) second review | ~20 min | +0 Rust | −0 TS; `bugfixes.json` 116 → 118 cases | (1) PRIMARY KEY/UNIQUE ART indexes make DuckDB refuse DROP COLUMN of any column before a constrained one ("an index depends on a column after it"), even with no CREATE INDEX; fix 10 now notes it (`customers.name` is a note, `customers.status` runs; both live). (2) Edit mode builds definitions with `isUnique: false`, so the UNIQUE rules can't fire until the port reports UNIQUE columns; README and plan say so. (3) A catalog attached under a default-catalog schema's name makes `"schema"."table"` ambiguous in DuckDB; documented with the USE follow-up.
- Task 16 | ~2 h 30 min (incl. a watchdog restart) | +~1,450 Rust (decode.rs ~700 incl. 8 unit tests; driver.rs +~200: `ResultReader` over `Statement::step()`, `decimal_param`, `open_session`, 2 unit tests; tests/values.rs ~550: 111 typed cells incl. 18 key round trips, ~28,600 values compared with `CAST(v AS VARCHAR)` twice; live.rs reworked for TIME_NS; `MAX_SAFE_INTEGER` re-exported from seaquel-engine), −1 dependency (`base64`) | −0 TS removed; +~25 TS (`binaryStringEncoding` drops duckdb, `+00`/`+02` offsets parse and are detected, BC timestamps no longer shown as year 2044) +~30 vitest | (1) The driver no longer uses duckdb-rs's `Row`/`ValueRef`: it executes, then reads each Arrow chunk from `step()` and decodes cells itself, guided by a `Kind` tree built once per statement from the logical types. That removes duckdb-rs's `unreachable!`/`unimplemented!` panics (TIME_NS reads now) and the per-cell Debug dump of the whole column chunk: 100k rows with a LIST and a STRUCT column went from 1.86 s and 252 MB of text to 0.19 s (25k/50k/100k: 65/101/192 ms, debug build). (2) Without `arrow_lossless_conversion` DuckDB sends TIMETZ as its local time only (`TIMETZ '12:00:00+02'` = `10:00:00+00` is false, so the offset matters for keys), and HUGEINT/UHUGEINT share DECIMAL(38,0)'s carrier; the driver sets it on connect. It also turns BOOLEAN into `arrow.bool8` (an Int8), which the first run decoded as `Int(1)`. The lossy forms still decode (a second CAST comparison runs with the setting reset). (3) Every temporal text matched DuckDB's `CAST(v AS VARCHAR)` on the first run over ~26k generated values (BC and 5-million-year dates, infinities in every TIMESTAMP unit, TIMETZ offsets with seconds, intervals with per-part signs and hours past 24); TIMESTAMPTZ is always UTC `…+00`, which is what DuckDB prints under `TimeZone='UTC'` and binds back in any zone. (4) A DECIMAL parameter must be at least one digit wider than its scale: DECIMAL(2,2) prints `-.05`. (5) The workspace serde_json has no `preserve_order`, so a STRUCT's JSON keys come out sorted; enabling it would change MySQL EXPLAIN walking order workspace-wide, so it wasn't. (6) V8 parses `0044-03-15 (BC) 12:00:00` as 2044 (the parentheses are a comment); `formatDateTime` now only hands strict ISO shapes to `new Date()`. (7) No SQL value fails to decode any more, so the "batch, then UNSUPPORTED_TYPE" stream test became a unit test that forces a column's kind.
- Task 14 | ~1 h 45 min | +~1,050 Rust (decode.rs 300 new incl. 6 unit tests, bind.rs 230 new incl. 2 unit tests, tests/values.rs 520 new: 67 typed cells, NULL into 21 column types, unsupported types, CRUD on 9 key types; driver.rs −95 (old JSON decoder and binder), smoke.rs binary-key and datetime2(7) cases flipped; `base64` and `chrono` deps dropped) | −~10 TS removed (MSSQL explain bind-skipping, `mssql` in `binaryStringEncoding`); +~95 src (`mssqlPaginate` in rust-engine-client.ts, `substituteMssqlParameters` in query-params.ts), +~150 tests (32 MSSQL pagination parity cases from paginate.json + bugfixes.json, 6 query-params cases) | (1) tiberius converts money to an f64 while reading the row and keeps no raw value, so the decoder recovers the count of 1/10000 units from the f64: exact below 2^39 ≈ 5.5·10¹¹ (a bit under the plan's 9·10¹¹; the f64 spacing reaches 0.0001 there), and MAX money reads one unit off (`…477.5808`). (2) No declared parameter type takes NULL into every column (nvarchar fails varbinary/image, varbinary fails float/date/time, …; checked with sp_executesql against 21 types), so the driver rewrites a `@Pn` whose value is NULL to the literal `NULL` (tokenizer skips strings, names, comments); the one visible change is `COALESCE(@P1, @P2)` with both NULL (error 4127). (3) datetime2(7) used to read with nine digits and datetime with nine (1/300 s as nanoseconds); dates and times are now formatted from tiberius's raw values in SQL Server's own form, trailing zeros dropped, datetime to the millisecond; datetimeoffset switched from RFC 3339 `T…+01:00` to SQL Server's `2024-01-02 03:04:05.5 +01:00` (local time, round-trips as a key). (4) MSSQL query parameters were inlined as `'…'` literals, which are varchar in the database's code page: `{{p}}` = 東京 read back `??`. They now bind as `@Pn` (named, so reused), a literal holding parameters binds whole, like MySQL's. (5) DuckDB still sends base64, so the `base64` encoding stays for it alone. (6) The E2E couldn't use the table editor's `addFromTable` directly (needs full app state); it copies its mapping.
- Task 14 review follow-ups | ~40 min | +~110 Rust (bind.rs: `inline_nulls` leaves a `@Pn =` in an EXEC argument list alone, side effects documented; introspect.rs: plain EXPLAIN declares decimals as `numeric(p,s)`; values.rs: money-max bind-back test, NULL tautology dropped; money docs corrected) | −~110 TS (`substituteMssqlParameters` removed); +~70 src/tests | (1) MSSQL `{{params}}` are inlined again (binding broke `TOP {{n}}`, `CREATE VIEW`, defaults, a user's own `@p1`, and varchar index seeks); strings now go in as `N'…'`, and an existing `'…'` literal that ends up holding non-ASCII text gets the `N` prefix, which is what fixed `東京` → `??`. (2) Money: sampling tiberius's own arithmetic shows the recovery is exact for |value| < 2^39 (≈ 2^52.3 units), not "2^39 units"; above 2^53 units the sum inside tiberius rounds, and near the ends of the range the error reaches ~1536 units. The maximum reads `…477.5808`: bound back it compares as numeric (no match) and should overflow on assignment (8115); pinned in a live test not yet run (Docker was hung).
- Task 14 re-review follow-ups | ~30 min | +~90 Rust (bind.rs EXEC unit test, which the previous round reported but a formatter-shifted anchor had silently dropped; values.rs `null_parameters_in_exec`, live: named arguments kept, `@P1 OUTPUT` with NULL fails with 179) | +~60 TS src (`substituteMssqlInline` now skips `--` and nested block comments, `[…]` and `"…"`, parenthesizes negative numbers, spaces a literal off a preceding word, writes exponent decimals as plain digits) +~50 tests | `SELECT 1 N'x'` parses as an alias on SQL Server 2022, so the `N` prefix needs no special case. The DuckDB inline path (`isInsideStringLiteral`) has the same comment/quoted-name flaw; left for the DuckDB work.
- Task 17 (port) | ~1 h 40 min | +~2,900 Rust (duckdb src: dialect.rs ~460, introspect.rs ~780, driver.rs +~95; tests: dialect_parity.rs ~460, introspect_parity.rs ~345, smoke.rs ~540; seaquel-engine ddl.rs +~150 (`AlterTableRules`, `generate_alter_table_sql_with`), crud.rs `_qs` builders, `Dialect::quote_schema`, `Driver::unique_columns`, shared `format_bytes`; rpc `uniqueColumns` + test) | −0 TS removed (duckdb.ts stays for the demo); +~140 TS (`qualified-table.ts`, `qualifiedTable` on both clients, duckdb in `RUST_ENGINES`/`PAGINATE`, `isUnique` from `uniqueColumns`, data tab uses `qualifiedTable`) +~200 vitest; E2E in the session scratchpad (`t17/e2e/`) | (1) Parity: 0 of 146 recorded cases caught a port difference that wasn't a planned fix; the 79 `replaces` all changed the TS output as asserted, and every hand-written case matched on the first run, live ones included (18 on a temp copy of the seeded file, which stays byte-identical). (2) The new DuckDB DDL switches (`not_null_after_add_column`, `indexes_block_column_changes`, `constraints_block_column_drops`) and the schema quote went into a separate `AlterTableRules` and `_with`/`_qs` builder variants, not `AlterTableOptions`: every engine builds that as an exhaustive `const`, and MSSQL's crate was mid-edit. Postgres/MySQL/SQLite/MSSQL parity unchanged. (3) `isUnique` travels as `uniqueColumns` on the `tableMetadata` response (`Driver::unique_columns`, default none) instead of a `SchemaColumn` field, for the same reason (MSSQL builds `SchemaColumn` literals); `RustEngineClient.tableMetadata` sets `isUnique` on the TS columns. Single-column UNIQUE constraints only. (4) The 28 ALTER scripts run to the end live, statement by statement; a DuckDB EXPLAIN takes bound `?`/`$n` parameters natively. (5) Not changed: `schema-tab.svelte` DROP/TRUNCATE and `table-node.svelte` still quote a listed `catalog.schema` as one name (Task 18), and the DuckDB `{{param}}` inline path in `query-params.ts` keeps its comment/quoted-name flaw (the file was another agent's).
- Task 17 review follow-ups | ~50 min | +~250 Rust (SchemaColumn `is_unique`/`in_unique_constraint`, CreateTableColumn `in_unique_constraint`, ddl `in_constraint` + 1 test, DuckDB UNIQUE query merged into `table_metadata`, 2 smoke tests; `Driver`/`Core::unique_columns` and rpc `uniqueColumns` removed) | +~150 TS (`substituteDuckdbInline` + 6 tests); ci.yml seeds DuckDB in the engines job | (1) Composite UNIQUE: DuckDB refuses `ALTER COLUMN a TYPE` and a `DROP COLUMN` before any UNIQUE column, composite too; the rules now read `in_unique_constraint`, both review cases pass live. (2) `seed.mjs duckdb` dry-run into a scratch mirror: a freshly seeded file passes every live case; `SeededCopy` panics under `SEAQUEL_TEST_REQUIRE_ENGINES` and takes `SEAQUEL_TEST_DUCKDB_FILE`. (3) DuckDB supports nested block comments, `$tag$…$tag$` and `E'…'` strings; the inliner handles all three. Desktop/web DuckDB still inlines `{{params}}` (nothing binds them), like the demo.
- Task 18 (0-row edits) | ~1 h 30 min | +~200 Rust (`ExpectRows` + `BatchStatement.expect_rows` (`expectRows`, serde default) + `DbError::no_rows_affected` in seaquel-types; checked before COMMIT in `impl_sqlx_driver!`, MSSQL `run_transaction` and DuckDB `transaction_blocking`; testkit `run_expect_rows` in every engine's smoke run; server maps `NO_ROWS_AFFECTED` to 409) | +~120 TS src (`stale-edit.ts`, direct-path check in `query-crud`, `executeAll` check + `failedChangeId`, sheet keeps failed changes and highlights the failed one, data viewer no longer ignores a failed delete, DuckDB-WASM `rowsAffected` reads `Count`) +~130 vitest; 1 en.json key | (1) The pending-changes path doesn't go through `transaction()`: `executeAll` runs each change with `execute` and has `rowsAffected`, so it checks there (a 0-row statement changed nothing, so nothing needs rolling back). `expectRows` is implemented and tested in all five drivers but nothing in the UI sends it yet. (2) The error's index is in the message (`Statement 2 (index 1) …`), not a new `DbError` field, since `DbError` is built as a literal in ~25 places. (3) sqlx-mysql 0.8.6 always connects with `CLIENT_FOUND_ROWS`, so a same-value UPDATE counts 1 on MySQL and MariaDB (smoke, live); no change needed. DuckDB (Rust and WASM) and MSSQL count it too. (4) DuckDB-WASM's `numRows` was always 1 for DML (the `Count` answer), so the demo never saw a 0; it now reads the count. (5) Known 0-count writes: Postgres DO INSTEAD rules, SQLite INSTEAD OF triggers on views, MSSQL with `SET NOCOUNT ON` left on the session.
- Task 18 (UI snippets) | ~1 h 45 min | +~560 Rust (ddl.rs `UniqueChanges` in `AlterTableRules`, `pg_drop_unique`, `tsql::drop_unique`, 3 unit tests; each dialect picks a variant; live UNIQUE tests in every engine's smoke.rs, MSSQL row-count test) | +~330 TS src (`quoteIdent`/`plainQualifiedTable`/`editorQualifiedTable`/`selectPreview` in `qualified-table.ts`, `EngineClient.quoteIdent`, `sql-scan.ts` tokenizer with `hasRowLimit`/`countQuery`, `utils/column-type.ts`; call sites in data-tabs, schema-tab, table-node, command-palette, completions, query builder, export) +~330 vitest | (1) `quoteIdent` matches every engine's recorded quote.json; `legacyQualifiedTable` is gone, so MySQL table names escape backticks and SQL Server `]`. (2) Row-limit detection is top-level only now: a LIMIT inside a subquery no longer stops paging, and OFFSET/FETCH FIRST count on every engine. The MSSQL count wrapper had never stripped ORDER BY (the count failed and fell back to an estimate); it does now, checked live with ORDER BY in OVER() and a TOP subquery. (3) UNIQUE: Postgres and SQL Server add and drop (catalog lookup), MySQL adds (drop is a note), SQLite and DuckDB notes (both reject the statement, checked live). Only DuckDB reports `is_unique`, so the drop lookups only fire from definitions that say so. (4) The demo's `DuckDBAdapter.quote` stays unescaped: the frozen TS fixtures record it (plan follow-up).
- Task 18 (UI snippets) review follow-ups | ~1 h 15 min | +~450 Rust (`introspect::apply_unique_indexes` + 1 test, called in the PG/MySQL/MSSQL/SQLite `table_metadata`; `UniqueChanges::DropIndex`/`AddAndDropIndex`, PG and T-SQL drops now cover plain unique indexes, +1 ddl test; testkit `editor_definition`/`run_unique_checkbox`; live `unique_checkbox_from_metadata` per engine; the recorded-columns comparisons in the PG/MySQL/SQLite introspection tests and MSSQL `bugfix_cases_live` add or clear the derived flags) | +~180 TS (`reserved-words.ts`, `insertTarget`, MySQL `#`) +~70 vitest; fixtures byte-identical | (1) Reserved words: generated from `pg_get_keywords()` (R+T), MySQL 8 `KEYWORDS.RESERVED`, `duckdb_keywords()` (reserved+type_function), SQLite's and T-SQL+ODBC's documented lists, then checked live on each engine against the union of all lists (~1,150 words, `SELECT 1 FROM w.t` and `FROM s.w`): every syntax error was in its engine's list. MySQL and MariaDB accept every reserved word next to a dot. (2) UNIQUE comes from the index metadata (no new query): a single-column unique index that isn't the primary key's, constraint or not, is `is_unique`; any unique index marks its columns `in_unique_constraint`. Unchecking drops it: Postgres and SQL Server look it up and drop it as a constraint or an index, MySQL `DROP INDEX name ON t`, SQLite `DROP INDEX` unless it is an autoindex (note).
- Task 18 (UI snippets) re-review minors | ~20 min | +~200 Rust (PG `PARTIAL_UNIQUE_SQL`, MSSQL `FILTERED_UNIQUE_SQL`, one small unrecorded query each in `table_metadata`; PG and T-SQL drops skip partial/filtered and INCLUDE indexes; 2 live tests; MSSQL `bugfix_cases_live` checks the flags against an explicit list) | +0 TS | The recorded index queries carry neither the predicate nor INCLUDE columns, so a separate query names those indexes and `apply_unique_indexes` leaves them out; fixtures untouched. The MSSQL scratch schema's filtered `fx_customers_email_uq` had been reported as UNIQUE.
- Task 18 (UI snippets) SQLite partial indexes | ~15 min | +~110 Rust (`partial_indexes` reads the `partial` column the recorded `INDEXES_SQL` already selects, so no new query; ddl.rs drops a column's unique index only when the definition has exactly one on it, else a note; 1 live test) | +0 TS | The definition can't tell a partial index from a plain one, so a column with both gets the note instead of a guess.
- Task 19 | ~1 h 30 min | +~110 Rust net (shared `Row::text`/`non_empty`/`truthy` and `introspect::truthy` +~60 incl. 1 test; `ddl::AddColumnOptions` + `generate_add_column_ddl` taking a quoted table, +~100 incl. 1 test; −~130 duplicated lines across the five `introspect.rs` and DuckDB's `add_column`) | −2,053 TS in `src/lib/db` (5,660 → 3,607: `mysql.ts`, `sqlite.ts`, `mssql.ts`, `explain-helpers.ts`, `sqlite-casts.test.ts` deleted; `crud-helpers.ts` 219 → 102, `alter-table.ts` 252 → 221, `index.ts` 170 → 155), −144 in `src/lib/engine` (2,729 → 2,585), −2,465 in `scripts/fixtures` (4,247 → 1,782: the MySQL, SQLite, SQLite-demo and MSSQL corpora and the ShowPlan capture; all but the throwaway SQLite-demo corpus kept in `docs/plans/artifacts/2026-09-27-*`); `jsdom` and `@types/jsdom` removed | (1) `getAdapter` serves only `duckdb`; `DatabaseAdapter` is now DuckDB's surface with every member required and no cast lookups, so `TsEngineClient` lost its optional-member fallbacks, the SQLite ANALYZE timing, the bind-values switch and `connectionName`. (2) An inherent `Row::text` would silently shadow a trait method of the same name, so the Postgres and MySQL readings were renamed (`printed`/`opt_printed`, `catalog_text`/`opt_catalog_text`) rather than left as `text`. MSSQL's old `truthy` treated a Float 0 as true; the shared one follows JS (no catalog column is a float). (3) Every fixture JSON is byte-identical to p2-t18; only the READMEs changed (frozen notice). `mssql`, `mysql2` and `better-sqlite3` stay: the e2e seeders and the web server use them. (4) `npm run build:web` ran out of Node's default heap in adapter-node; it passed with `NODE_OPTIONS=--max-old-space-size=8192`; the review found the p2-t18 baseline fails the same way (the Dockerfile already sets 8192). (5) A workspace-wide `cargo fmt` reformatted ~30 files outside the task (layout only).
- Task 20 | ~35 min | +0 Rust | +0 TS; ci.yml, release.yml, docker-publish.yml | (1) The engines job ran only `--test smoke` and seeded only DuckDB, so MSSQL's `live`/`values`/`row_cap`/`live_tests` and MySQL/MariaDB `values` never ran against a server in CI, and the MSSQL tests had no `seaquel_test` database (the service image can't create one) while MySQL's EXPLAIN smoke reads the seeded `users`/`orders`. It now seeds postgresql, mysql, mariadb, sqlserver and duckdb (as `npm run e2e:db:seed`) and runs every test target of the five engine crates under `SEAQUEL_TEST_REQUIRE_ENGINES=1`; locally all 28 binaries pass and nothing prints `skipping`. (2) `cargo fmt --all --check` added (clean). The types:gen staleness check already existed (uses `git status --porcelain`, so it also catches new untracked files). (3) Every job has `timeout-minutes`. (4) release.yml builds only the Tauri frontend (`npm run build`, static adapter), which passes with its `--max-old-space-size=4096` (≈5.8 GB peak RSS either way); only the Dockerfile builds web and it sets 8192.
- Task 21 | ~55 min | +0 Rust (doc comments only: the 16 `dialect.rs`/`introspect.rs`/`*_parity.rs` headers of MySQL, SQLite, MSSQL and DuckDB say which TS file was deleted, or call DuckDB's the demo's `duckdb.ts`) | −1,782 TS (`scripts/fixtures/` deleted: recorder core, DuckDB corpus, tsconfig; the `fixtures:record` npm script) | (1) The recorder and the DuckDB corpus are copied byte for byte to `docs/plans/artifacts/2026-09-27-recorder-*.txt` (7 files) and `2026-09-27-duckdb-fixture-corpus.ts.txt`; check mode passed for DuckDB (15 tests) just before the delete. Nothing else referenced `scripts/fixtures`: vitest found it by its default `*.test.ts` glob, oxlint through the directory's own tsconfig, and oxfmt had no entry. (2) The design doc keeps its phase numbers: the SQL tooling split off phase 2 is "phase 2b", because "phase 3" to "phase 8" are referenced in the phase 0 plan, the DuckDB crate's Cargo.toml and the phase 2 plan. (3) The DuckDB bugfix model and WASM check artifacts still import `scripts/fixtures/*` by absolute path; the DuckDB README says where those files went.
- Task 22 | ~1 h | +0 Rust | −0 TS; design doc "Phase 2 cost" section, a status line in the plan | (1) Line counts are `git diff --no-index --numstat` of `crates/`, `src/` and `src-tauri/src` against the p2-t0 snapshot, with fixture JSON, test files and top-level `#[cfg(test)]` modules split out; `src/lib/paraglide` (generated, gitignored) and the `cargo fmt` reflow in `src-tauri` left out. (2) The effort log folds review time into Tasks 2, 3, 6 and 11, so the review share (~9.3 h of ~39.8 h) is a floor. (3) The bugs outside the numbered fix lists are counted from this log's surprises; which ones came from a live test and which from a review isn't always stated, so those counts are approximate and the doc says so. (4) The node-sql-parser consumers phase 2b replaces (~3,900 lines) have almost no unit tests, and the lessons store criteria, not solutions: a baseline has to be written before anything can be compared, which is why the estimate is wide and a spike comes first.
