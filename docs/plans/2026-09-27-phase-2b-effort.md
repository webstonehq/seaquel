# Phase 2b effort log (task | wall time | Rust lines added | TS lines removed | surprises)

- Phase 2b spike | ~1.3 h (agent wall time; the timebox was 4–6 h) | +~1,980 Rust in `spike/`, not shipped (`sql-spike` ~1,900: tutorial parser 712, visual AST 528, column sources 119, helpers ~140, scanner/offsets ~150, three probe binaries ~220; `wasm-stub` 75) | −0 TS removed; +~1,430 TS/JS in `spike/` (corpus, three vitest harnesses, Node bench, two Playwright scripts, build script) | (1) The lesson criteria never read the AST: `criteria.ts` works on the builder snapshot and the SQL text, so the tutorial `ParsedQuery` port reached parity on all 91 tutorial entries with no criterion rewritten. The only mapper fixes were keeping a decimal's text (`50.0`) and upper-casing aggregate names. (2) The TS visual AST is broken under node-sql-parser 5.x and it shows in the product: columns print as `[object Object]` (144 of 172 entries), a query without LIMIT gets `count: NaN`, DISTINCT is never detected, window calls print as `""`. The port fixes them as a side effect. (3) The query builder always parses as PostgreSQL. sqlparser's PostgreSQL dialect rejects backticks where node-sql-parser accepted them, so the builder has to pass the engine dialect in 2b; with it, SQL Server bracket names round-trip too, which they don't today. (4) Tauri's CSP (`script-src 'self'`) blocks WebAssembly in WebKit and Chromium; it needs `'wasm-unsafe-eval'`. Everything else in the build was plain `?url` + `init()` in the root `+layout.ts`, with no Vite plugin. (5) The module is 1.56 MB (561 KB gzip) and split-only saves just 6%, because sqlparser's dialect trait objects pull the parser in with the tokenizer. It's still about 140 KB gzip more than the node-sql-parser it replaces. Init takes 3–7 ms in Chromium and 16–27 ms in WebKit. (6) The editor's `splitSqlStatements` ignores its `dbType` and gets 10 of 15 per-engine quoting cases wrong (MySQL `#`/backticks/backslashes, SQL Server brackets, `E''`, nested comments). `engine/sql-scan.ts` gets all 15, and sqlparser's tokenizer gets 12 but errors on unterminated input, so the recommendation is to port the hand scanner. (7) Positions come in three units, not two: sqlparser spans count chars, not bytes or UTF-16 units.
- Task 1 (create the crates) | ~0.5 h (agent wall time) | +~680 Rust in `crates/seaquel-sql` and `crates/seaquel-wasm` (stubs with final signatures; ~200 of it is the spike's `ParsedQuery`/`ParsedQueryVisual` type definitions, moved over so the stubs have real types; `SqlEngine` with tests ~100), +~50 JS (crate rules and their tests, `vite.config.js`) | −0 TS | (1) `Cargo.lock` gained only sqlparser 0.63.0, ryu-js 1.0.3 and the two crates; the `=0.2.128` pin kept one wasm-bindgen. (2) `vite.config.js` had no `test` block, so `test.exclude` has to spread `configDefaults.exclude` or vitest would start collecting `node_modules`. (3) `npx oxlint --type-aware --type-check` fails on `spike/` (untracked, there before this task) and on Task 2's in-progress `scripts/sql-fixtures/`: `$lib` doesn't resolve outside `src/`. Clean with those two paths ignored. (4) `Statement::end` keeps the TS's "input length minus one" for the last statement; as a byte offset it can land inside a multi-byte char, which Task 3 and Task 8's offset conversion must handle.
- Task 2 (freeze the TS baseline) | ~0.5 h (agent wall time) | +0 Rust | −0 TS removed; +~2,400 TS in `scripts/sql-fixtures/` besides the moved `corpus.ts` (recorder 833, scanner corpus 429, visual model 554, split/statement/params models ~365, CREATE TABLE and acceptance corpora ~200) and 2.9 MB of fixtures (17 files) | (1) The plan's models covered fixes 1–4 and 10 only, but fixes 11–14 change hundreds of scanner-corpus cases (per engine), far more than a few hand-written cases, so they got models too (`statements-model.ts`, `params-model.ts`), and bugfixes.json's `modelCases` list every changed case with a reason and a diff. (2) The spike's "[object Object]" pattern hid formatting differences: the TS also prints `x BETWEEN (1, 5)`, `CAST(x AS unknown)` for `CAST` and `::`, `$1` as `""` and `!=` as written, which the spike's port doesn't; visual.json pins the TS forms (decision 2), so Task 6 has more to settle than the six differences the spike listed. (3) A query calling any non-aggregate function (UPPER, COALESCE, …) gets no visualization at all: `detectAggregate` throws on 5.x's object function name. Put under fix 3. (4) `parseCreateTableSql` drops or loses data on our own generated DDL: `SERIAL` columns get type `""`, and any name with a doubled quote makes the whole parse `null`. Left at parity, proposed as fixes. (5) The running demo's Visual tab (a pre-Task-7 build) shows exactly what the recorded TS gives (`p.[object Object]`, `LIMIT NaN`, `BETWEEN (1, 5)`) on 10 spot-checked entries.
- Task 2 follow-up (checkpoint fixes 6+, 13+, 15, 16, 17) | ~0.3 h (agent wall time) | +0 Rust | −0 TS removed; +~330 TS (`create-table-fixed.ts`, fix 6/15 edits in `visual-fixed.ts`, forced inline in `params-model.ts`, recorder attribution by leaving one fix out) | node-sql-parser gives `:name` as a `param` node that the TS printed as `$name`, so fix 6 covers it too. `parseCreateTableSql` also dropped any index whose name or table holds a space (fix 17). Adding "15." after the fix 8 item made oxfmt renumber it to 9, so fixes 15–17 sit after 14 under their own headings.
- Task 7 (build and load the module everywhere) | ~0.9 h (agent wall time) | +0 Rust | −0 TS; +~250 JS (`scripts/build-wasm.mjs`), +~140 TS (`src/lib/wasm/`: init, vitest setup, 5 tests), config in `package.json`, `vite.config.js`, CI, `release.yml`, `Dockerfile` | (1) wasm-bindgen 0.2.128's `initSync` returns early once the module is set, so the plan's "re-instantiate with `initSync({ module })`" can't work as written, and `--experimental-reset-state-function` generates a reset that calls a `__wbindgen_start` this crate doesn't export (TypeError). The build script appends a 5-line `__seaquel_reinstantiate()` to the glue instead, and fails loudly if the glue's shape changes. (2) Cold `wasm:build` 13 s (fresh target dir), no-change rerun 0.2 s, warm rebuild after touching `seaquel-sql` 0.7 s with the `version()` stub. With the spike's full-size module as a proxy, a warm cargo rebuild is 0.8 s and wasm-opt `-Oz` (npm binaryen) 13 s, so ~14 s: no `--fast` mode. (3) Stub module: 13.6 KB, 6.1 KB gzip, 5.3 KB brotli; LTO drops sqlparser while only `version()` is exported. (4) Real Tauri window: a `tauri build --debug --no-bundle` binary loads the module from `tauri://localhost` as `200 application/wasm` under the new CSP (init 9 ms), and `tauri:dev` loads it from Vite (381 ms, cold). The old CSP gives a 500 page in Chromium and WebKit, so the root `load` really gates rendering. (5) Builds flaked with `Could not resolve "./ai_close.js" from src/lib/paraglide/messages/_index.js` when another agent's vitest/vite ran at the same time: paraglide writes `src/lib/paraglide` in a different output structure outside `vite build`, and the build's cache says "inputs unchanged". Not WASM-related; rerunning alone passes. (6) `docker build .` passed first try (wasm-bindgen-cli install 46 s, module 19 s) and the container serves the `.wasm` as `application/wasm`.
- Task 2 review fixes (fixes 10+, 11+, 13+, 14+, 16+, 17+, new 18) | ~0.6 h (agent wall time) | +0 Rust | −0 TS removed; +~450 TS (`scan-model.ts`, rewritten `statements-model.ts`, forced inline for MySQL, COLLATE and DuckDB-quoted three-part names, 60 scanner inputs, 2 CREATE TABLE cases) | The mysql and mariadb clients split on a `;` inside `/*! … */` (checked against the Docker containers), and MySQL treats `/*M!` as a plain comment, so only MariaDB runs it. Fix 18's number tokens also change `hasRowLimit` (`1=1LIMIT 5`, `3.limit`), so row-limit.json and count-query.json now come from the scanner model too. New `DestructiveReason` values (`drop_sequence`, `drop_function`, `merge_delete`) need labels and translations in Task 9.
- Task 2 re-review pass | ~0.3 h (agent wall time) | +0 Rust | +~150 TS (scanner number rules per engine, quoted and `U&` function names, 41 corpus inputs) | MySQL's `db.2fa_codes` also needed the `.` right after a name kept as the qualifier's dot, not read as `.2`. Blocklists stay incomplete by nature (DuckDB httpfs egress is allowed); the in-database read-only Follow-up is the real guard.
- Task 5 (`parse_create_table`) | ~0.6 h (agent wall time) | +~960 Rust in `create_table.rs` (~560 the port, ~330 a small backtracking matcher with JavaScript regex semantics, ~70 unit tests), +~180 in `tests/create_table_parity.rs` | −0 TS (`parse-create-table.ts`, 306 lines, goes when Task 10 switches the table editor) | (1) The TS isn't a tokenizer user: it's a dozen regexes over three scanners of its own (comment strip, paren match, top-level comma split), so the port keeps those scanners rather than using `scan::tokens`, and runs each regex through a ~330-line matcher with JS's backtracking order. The output depends on regex details a hand-rolled parser would drift from: `DEFAULT (.+?)` stops at the first ` NULL`-like word or a line end, `DEFAULT`/`PRIMARY KEY` match without word boundaries, and a doubled quote can also end a name when the longer reading fails. (2) 124 of 124 fixture cases matched on the first run. A throwaway differential run against `create-table-fixed.ts` on ~41,000 mutated corpus inputs (inserted quotes, brackets, comments, keywords, non-ASCII whitespace, case changes) also gave 0 differences. (3) Ids: the TS made random UUIDs, the port returns `column-N`, `index-N`, `fk-N` (unique per list, which is all the editor's keyed lists need). No type changes in `seaquel-types`: `collation` was already there.
- Task 3 (scanner and statement checks) | ~0.8 h (agent wall time) | +~1,815 Rust in `seaquel-sql` (`scan.rs` 731, `statements.rs` 590, `read_only.rs` 294, `js_ws.rs` 42, `js_word.rs` 158 including a 414-range table; about 250 of it is unit tests), +771 in `tests/scan_parity.rs` | −0 TS | (1) Every scanner fixture passed on the first run: split, statement-at (every byte offset), row-limit, statements and read-only 2,196 of 2,196 (366 inputs × 6 engines); count-query 2,160 of 2,160. The six models also gave the same output as the port on 60 adversarial inputs (10 per engine). (2) The one real difference the review found: `char::is_alphanumeric` isn't the TS's `\p{L}\p{N}`. The TS splits a word at a combining mark (so a Thai table name loses its vowel signs in `table_from_select`) and at a letter outside the BMP (two surrogates). The port keeps the TS rule with a BMP table generated by Node; the truncated name is a Follow-up, not a numbered fix. (3) `has_where` and `drops_column` rescanned to the end of the statement for every DELETE/UPDATE/ALTER, which is quadratic: 180 ms on a 200 KB `(…) UPDATE … ` statement. A memo of the last scan makes it 5 ms, with the same answers. (4) Native release: a 1 MB script (the six e2e schema files repeated) splits in 3 ms; the destructive, read-only and row-limit checks together take 14 ms (debug build: 30 ms and 54 ms). (5) The every-prefix property test is O(n²) on the ~4 KB schema files: 155 s in debug as one test, ~35 s wall for the whole crate once split into one test per engine. (6) `cargo fmt -p seaquel-sql` reformats the other agents' files in the crate; format your own files with `rustfmt` instead.
- Task 5 review fixes | ~0.4 h (agent wall time) | +~150 Rust (step budget, possessive `\s+`, per-class run cache, time-bounded regression test) | −0 TS | A `DEFAULT x` followed by a long whitespace run was quadratic: the greedy `\s+` before each keyword retried every shorter count at every step of the lazy `.+?`. It was about 45× slower than V8, so 50k spaces took 62 s. Possessive `\s+` (the next node never starts with whitespace, so the output is the same) and a cache of each class's current run make it linear. The reviewer's seven probes take 0.4–6 ms in release (V8: up to 1.3 s). A budget of 2M + 100 steps per character (ordinary DDL uses under 40) turns any blowup that's left into `None`. Differential against `create-table-fixed.ts`: 0 of 72,862 inputs differ. Ids stay placeholders; Task 8's wrapper swaps in UUIDs.
- Task 6 (AST helpers on sqlparser) | ~1.1 h (agent wall time) | +~2,390 Rust in `src/ast/` (tutorial/builder 950, visual 835, depth guard and helpers 440, column refs 105) and +~900 in `tests/ast_{parity,behaviour,nesting}.rs`; 29 generated TS types | −0 TS (Task 10 deletes it) | (1) Parity came almost free: the spike's port plus the TS forms decision 2 keeps matched every fixture on the first run except four non-tutorial entries of tutorial.json, which parse differently in PostgreSQL mode (backticks rejected; `OFFSET … FETCH`, `GROUP BY ALL` and `QUALIFY` accepted). The test lists them by name. (2) sqlparser's recursion limit doesn't cover operator chains: `a AND b AND …`, `a + b + …`, `x::int::…`, `… UNION …` are built in a loop as left-deep trees, and dropping 20,000 links overflows a 1 MB stack inside sqlparser itself. `ast::util::parse` now bounds the chain depth from the tokens first (the heaviest comma-separated segment per parenthesis group, summed down the nesting) and refuses more than 1,000 as a parse error. (3) In a real wasm32 build (Node, default 1 MB stack) nothing traps. Our own walkers at the cap first trap at 256 KB, but sqlparser's own 50-level recursion (10,000 nested `f(`) traps at 512 KB, so that margin is under 2×. Consider linking with `-zstack-size=2097152`. (4) sqlparser reads `!=` and `<>` as one operator; the Visual tab keeps the one written by recording each token's form during the parse. (5) ts-rs 12 makes a field `?:` only with `#[ts(optional)]` (or serde `default` + skip), not skip alone. With it, and `QueryFilter.operator` as `string`, all 17 generated types are identical to today's interfaces under a strict `tsc` equality check. (6) AST helpers alone, wasm-opt `-Oz`: 1.31 MB, 444 KB gzip, 350 KB brotli.
- Task 7 review fixes | ~0.6 h (agent wall time) | +0 Rust | −0 TS; +~120 TS/Svelte (`load-error.svelte`, root layout, init caching, 2 tests), +~60 JS in `build-wasm.mjs`, 6 `en.json` keys | (1) `callWasm` re-instantiates on any throw, not just `RuntimeError`: a stack overflow inside wasm is a `RangeError` in V8. (2) A failed init now shows a readable page with the error, a copy button and "Try again" instead of SvelteKit's 500; `/login`, `/signup` and `/airgap-setup` render anyway. Checked with Playwright for a 404 on the `.wasm`, a CSP block, and a transient 404 that "Try again" recovers from. (3) The Docker wasm stage no longer copies `rust-toolchain.toml` (it pulled a second toolchain); the wasm-bindgen-cli install layer is keyed on a one-line version file, and `cargo chef cook` caches the wasm deps. (4) The full check list failed twice mid-run because Task 3 was mid-edit in `seaquel-sql` (`crate::js_ws` missing, rustfmt); rerun later, it passed.
- Task 4 (`{{param}}` substitution) | ~0.7 h (agent wall time) | +~960 Rust in `params.rs` (~850 the port, ~110 unit tests), +225 in `tests/params_parity.rs` | −0 TS (`query-params.ts`, 715 lines, goes when Task 9 switches the callers) | (1) 1,470 of 1,470 fixture checks pass (366 extract, 184 substitute × 6 engines), 8 of them against a pinned Rust output: a JS `bigint` inside ±(2^53−1) (`{"$sq":"bigint","v":"-1"}`) decodes to `Value::Int`, the same as the number `-1`, so it takes the number's path (inline `(-1)` where the TS wrote the string `'-1'`). A bigint beyond i64 comes back as a `decimal` bind value for the same reason. No caller can produce either: the only source of values is `coerceValue` in `parameter-input-dialog.svelte` (null, number, boolean, string), so none passes bytes, JSON or arrays either (decision 11; the port refuses them when a value is used). (2) Every engine path runs on Task 3's tokenizer instead of its own loop. 33 adversarial inputs × 6 engines × bound/inline against `params-model.ts`: 11 of 396 differ, all of them tokenizer rules, not substitution: `/*M! … */` is code on MariaDB's bound path too (the TS copied it, leaving `{{p}}` in SQL MariaDB runs); DuckDB `--` ends at `\r` (fix 10); DuckDB `éE'…'` is a name plus a plain string; a dollar tag over 64 UTF-16 units is a word (sql-scan.ts's limit; the old DuckDB loop had none); and the bigint-beyond-i64 bind value above. (3) `ryu-js` matches `String(n)` on every case tried (`1e+21`, `-0` → `0`, `1.5e-7`, `5e-324`, `NaN`, `Infinity`). `plainDecimal` keeps the exponent if padding would pass 2^20 zeros, where JS would build a huge string or throw.
- Task 2 follow-up for the Task 3/4 reviews (fix 19, fix 10 and 13 extensions) | ~0.4 h (agent wall time) | +0 Rust | +~250 TS (per-engine word rule, fix 10 switchable, `params-model.ts` rewritten so every engine path runs on the shared scanner, 14 corpus inputs) | Checked on MySQL 8.4 and MariaDB 11 that `--` + NBSP isn't a comment and that `--` + TAB, VT, 0x1F and 0x7F are. Porting MySQL's bound path, SQL Server and DuckDB onto the scanner reproduced the TS output on every recorded case except the intended fixes. `INTÓ` (precomposed) is a name on every engine; the dual word-rule scan catches `INTO` + U+0301.
- Task 7 stack size | ~0.2 h (agent wall time) | +~20 Rust (`crates/seaquel-wasm/build.rs`) | −0 TS; +~60 JS (a wasm global-section reader in `build-wasm.mjs`) | The module links with a 2 MiB stack (`cargo::rustc-link-arg-cdylib=-zstack-size=2097152`, wasm32 only) from a build script rather than `RUSTFLAGS` or `.cargo/config.toml`: it touches no other crate's fingerprint, and the Docker stage (which runs cargo itself and doesn't copy `.cargo/`) gets it for free. `build-wasm.mjs` reads `__stack_pointer`'s initial value (the stack comes first on wasm32-unknown-unknown) and fails unless it's 2 MiB; binaryen's `wasm-dis` agrees (`(global $global$0 (mut i32) (i32.const 2097152))`, 1048576 before).
- Task 6 review fixes | ~0.6 h (agent wall time) | +~150 Rust (iterative HAVING and postfix-chain printing, pinned deviations, cap tests) | −0 TS | Measured on a 1 MB native release stack with the cap off, the first overflow is a WHERE/HAVING `OR`/`AND` chain at ~5,080 terms (~10,160 by the guard's count; the Visual filter tree nests per term). Once the printer walks casts, IS/IN/BETWEEN/LIKE and `AT TIME ZONE` iteratively, `+`, `::`, `LIKE` and `AT TIME ZONE` chains reach ≥16,500, `UNION` 22,000 and `IS NULL` 33,000. So the cap went from 1,000 to 2,000 (a 1,000-term `id = n OR …` is exactly at it), a 5× margin. In wasm32, every chain at the cap parses on a 512 KB stack. Surprise: plain sqlparser overflows a 1 MB *native* stack on nested FROM subqueries from depth 25, before its own recursion limit; in wasm that's a parse error at 1 MB and a trap only at 512 KB, so the 2 MB module stack covers it.
- Task 3 review fixes and fix 19 port | ~0.6 h (agent wall time) | +~450 Rust (`js_word.rs` now 370 with generated `\p{L}`, `\p{M}`, `\p{N}` tables of 1,154 ranges, ~9 KB; one-pass `where_follows` and EXPLAIN tables in `statements.rs`; fix 19 and fix 10 rules in `scan.rs`; a `ts_words` reading for the read-only check; timed and differential tests) | −0 TS | (1) The re-recorded fixtures passed on the first run: 2,280 cases per scanner file, count-query 2,244. The reviewer's probes (~194k cases) and a new 40k fix-19 fuzz give 0 differences against the models. (2) `destructive_reason` is linear now: 690 KB of nested `(DELETE … WHERE 1)` takes 7 ms, where it was 1.2 s. (3) A multithreaded `cargo test --lib` aborted once with SIGABRT while another agent's lib code was mid-edit, and passed on every rerun.
- Task 4 follow-up (re-recorded fixtures, fixes 19 and 10+) | ~0.2 h (agent wall time) | ~±60 Rust in `params.rs` (MySQL bound run and introducer on tokens) | −0 TS | Only MySQL/MariaDB bound needed changes: a literal run now binds only when a `{{p}}` is written in its tokens (`'{{p' '}}'` stays as it is), and the introducer is the previous word token (`é_u '…'` and `@_x'…'` are no longer introducers). The rest already matched the rebuilt model. 1,544 of 1,544 fixture checks (8 are the pinned small-bigint exception). 38 adversarial inputs, 456 comparisons: 4 differences, all a bigint beyond i64 binding as `decimal` (a `Value` limit). One SIGABRT in a parallel lib-test run didn't come back in three reruns, which all passed; another agent was editing the crate at the time.
- Task 4 review round (fix 13 value rules) | ~0.8 h (agent wall time) | +~330 Rust in `params.rs` (~150 of it unit tests; now 1,265 lines), ~±60 in `tests/params_parity.rs` | −0 TS; +~190 TS (`params-model.ts` value rules, recorder attribution per rule, 9 corpus inputs) | (1) Four value rules, each switchable in the model, so the recorder names the rule behind every changed case. `tag-edge`: a `$tag$` string must still end where it did once filled, which catches the live `$$Cost: ${{p}}$$` + `$ || 'INJ' --` breakout, the other edge, partial tags and unterminated strings. `escaped`: `\{{p}}` in `E'…'` and MySQL strings is text, not a parameter. `decimal`: an inlined decimal must match the decimal pattern (`NaN` refused). `int-digits`: bigints inline as digits. (2) Re-recorded twice, byte-identical. params.json 574 → 617 cases (9 new inputs); 4 existing cases changed (bigint inline); bugfixes.json 1,424 → 1,506 model cases, all 86 new or changed ones fix 13 with the rule's reason. The first attempt broadened the shared fix-13 reason and touched all 266 fix-13 model cases, so the reason stays and each rule adds its own. (3) Rust 1,757 of 1,757; 8 of them compare bind values as `Value`s (a JS bigint within 2^53 and a number past it both decode to `Value::Int`). Adversarial diff 46 inputs, 552 comparisons: 8 differences, all a bigint beyond i64 that `Value` decodes as `Decimal` (quoted inline, a `decimal` bind value). The reviewer's probes: every tag-edge value is refused on PG and DuckDB, and `1e18` inlines as digits. `1.5e19` is still quoted (`Value` makes it a `Decimal`). (4) Extraction stays engine-agnostic, so the dialog still prompts for an escaped `\{{p}}`, as for one in a comment (Follow-ups).
- Task 4 review round 2 (fix 13 `adjacent`) | ~0.5 h (agent wall time) | +~110 Rust in `params.rs` (`Out` and a unit test; 1,364 lines now) | −0 TS; +~60 TS (model `Out`, a rule reason, 3 corpus inputs) | (1) The reviewer's randomized pass found a quoted value inlined right after a quote merging with that literal through `''` (`E'a'{{p}}` with `\') OR 1=1 --`), which executed on the DuckDB CLI. Now a space goes before it, and after a value ending in a word character when a word or `$` follows (`NULL$$`, `1$t$`). (2) Recorded twice, byte-identical. params.json 617 → 638 cases; 7 existing cases changed (spacing only); bugfixes.json 1,506 → 1,585 model cases, all 79 new or changed ones fix 13. The first recording had an empty reason for the new rule: vitest doesn't type-check, so the `Record<ValueRule, …>` gap only showed in the output. (3) Rust 1,868 of 1,868 on the first run. Reviewer's `prop 600000` (7.16M checks): on the default reading every failure is Postgres bound; with the cases whose `$n` is glued to a `$` or word set aside (76,547 checks), 0 failures. The `ts_words`, `pg_backslash` and `mysql_ansi` readings still report failures; they aren't the sql_mode or reading we substitute for.
- Task 8 (exports, offsets and `src/lib/sql`) | ~0.8 h (agent wall time) | +~960 Rust in `crates/seaquel-wasm` (`lib.rs` 616: 16 exports, the `{ok}`/`{error}` envelope, a feature-gated `__test_trap`, ~250 of it tests; `offsets.rs` 367, ~200 of it tests) | −0 TS (Tasks 9 and 10 delete it); +~460 TS in `src/lib/sql/` (`index.ts`, `parameters.ts`) and ~1,300 in six test files | (1) Every fixture file passed through the TS wrapper on the first run, `statement-at.json` at every UTF-16 offset on every engine (3.5 s); the only changes to the copied tests are decision 11 (a bytes value is refused; the TS inlined `0x…`/`from_hex`) and `sqlTokens`/`stripTrailingOrderBy` checked through `countQuery`/`hasRowLimit`, as they're internal now. (2) The real trap test builds its own debug module with a `test-trap` feature (5 s cold, 0.2 s wasm-bindgen) and swaps it in with `vi.doMock`, so the release module has no trap export; it needs the wasm toolchain at test time (skipped without it, fails in CI). A panic is `RuntimeError: unreachable`, small-frame recursion a V8 `RangeError`, 64 KiB frames past the 2 MiB shadow stack `RuntimeError: memory access out of bounds`; `callWasm` recovers from all three. (3) `JSON.stringify` writes a lone surrogate as a `\uD83D` escape, which serde_json refuses, so JSON arguments (parameter values, table names) get U+FFFD in the wrapper first, as wasm-bindgen does for plain string arguments. (4) Module with everything exported: 1,710,767 B raw, 628,214 B gzip -9 (the review's figures; `build-wasm.mjs` prints 1,670.7 KiB, 619.3 KiB gzip, 473.1 KiB brotli), +71 KB gzip over the spike's 561 KB. twiggy shows sqlparser unchanged at about 1.08 MB; the growth is our own code (the ported scanner, checks, substitution and CREATE TABLE parser, and the Unicode word tables). (5) Keystroke rate: 100 KB script split 0.6 ms, statement at cursor 0.5 ms; 1 MB 5.9 ms and 4.8 ms, a little faster than the TS splitter (0.7 ms, 7 ms).
- Task 8 review fixes | ~0.4 h (agent wall time) | +~30 Rust (ts-rs derives on the four boundary shapes, a `ts` feature on `seaquel-wasm`, which `types:gen` now includes) | −0 TS; +~150 TS (`orFallback`, `parseVisualQuery`, `failures.test.ts`) | The wrapper keeps the TS's answers when the module fails, trap or bad call: the per-keystroke and `$derived` functions return their "couldn't parse" value and log, `validateReadOnlyQuery` fails closed with its refusal, `substituteParameters` wraps anything as `ParameterSubstitutionError`, and the run-path checks (`isDestructiveStatement`, `findDestructiveStatements`, `hasRowLimit`, `countQuery`) throw so the runner reports it. The `{ok}`/`{error}` envelope stays hand-mirrored in TS: the Rust side serializes borrowed generic structs that ts-rs can't export as one type. The Task 9 and 10 caller lists from the review are in the plan.
- Task 9 (switch the editor and query runner) | ~0.9 h (agent wall time) | +0 Rust | −~1,090 TS (`db/sql-parser.ts` 254, `db/query-utils.ts` 112, `engine/sql-scan.ts` 195, the originals `db/query-params.test.ts` 343 and `engine/sql-scan.test.ts` 149, the read-only check ~30, `query-file-parser.ts`'s own `{{name}}` regex 13); +~250 TS (two call-path test files, 9 cases; 5 read-only cases; run-path error handling) | (1) `db/query-params.ts` stays: `standalone-query-editor.svelte` (Task 10) still imports it; `db/column-sources.ts` stays for Task 10 too, though nothing in `src/` imports it now. (2) The AI params already carried an optional `databaseType` for the system prompt; it's now required (`DatabaseType \| undefined`) and the tool refuses every query when it's undefined or not an engine the module knows. (3) Only two run-path sites could throw outside a `try`: the row-limit check in `executeCurrent` and in paging (`executeStatementAtIndex`); a throw there would have left the tab spinning. `countQuery` sits inside the existing count `try`, so a failure gives the estimated total, and no rewritten SQL runs. (4) The demo smoke (Chromium) passed all four checks; a `parentNode` page error and Monaco's `doValidation` worker errors appear on load before anything runs. (5) One full `npx vitest run` failed `parity.test.ts`'s every-offset case on the 5 s timeout (3.8 s alone) while another agent was building; the rerun passed.
- Task 10 (Visual tab, query builder, tutorial and table editor; drop node-sql-parser) | ~0.5 h (agent wall time) | +0 Rust | −2,232 TS (`tutorial/sql-parser.ts` 1,213, `db/sql-ast-parser.ts` 532, `db/parse-create-table.ts` 306, `db/column-sources.ts` 181), ~−85 in `types/visualize.ts` (now re-exports the generated types); +143 TS (`tutorial/criteria.test.ts`) | (1) `criteria.test.ts` passed on the first run: 91 of 91 lesson verdicts and all 57 round trips plus the fix 9 cases regenerate the recorded SQL. (2) The standalone editor's only executor is the tutorial's DuckDB one, so it parses in `qb.engine` (default `postgres`), not `executor.dbType`, to keep the tutorial in PostgreSQL mode (decision 4). (3) Dropping node-sql-parser breaks the recorder in `scripts/sql-fixtures`, which imports it and the deleted TS, so it's out of vitest and oxlint until Task 12 archives it. `spike/` still imports it (already excluded). (4) Bundle, desktop and demo alike (the Task 8 snapshot vs now, Node gzip -9): JS 27,780,263 → 25,132,070 B raw (−2,648,193), 6,250,935 → 5,734,714 B gzip (−516,221); the `.wasm` is unchanged at 1,710,767 B raw, 634,203 B gzip (all `.wasm` 2,369,177 B), since the module already shipped from Task 7. The "after" build also lacks Task 9's deleted scanners, a few KB. (5) `npm run wasm:build` quietly keeps a stale `pkg/` when `wasm-bindgen` isn't on PATH and still exits 0, so a check run can pass without building; `WASM_BINDGEN=<path>` fixed it here. (6) Demo smoke (Chromium): the Visual tab on a join with a CAST and ROW_NUMBER() OVER shows `CAST(p.price AS INTEGER)` and `p.price > 10`, no `[object Object]`, `NaN` or LIMIT node (the Task 8 build shows all three); the builder round-trips `demo.products` with a filter, a canvas edit regenerates the SQL, and unparseable SQL now stays in the panel's editor; tutorial intro-1 completes from typed SQL; the table editor reads SERIAL, a column after a comment, `CONSTRAINT … PRIMARY KEY` and an inline REFERENCES.
- Tasks 9/10 review fixes | ~0.6 h (agent wall time) | +0 Rust | +~60 TS in `$lib/sql` (`splitSqlStatementsOrThrow`, `getStatementAtOffsetOrThrow`, `detectQueryTypeOrThrow`, exported `READ_ONLY_REFUSAL`), `resolveQueryOrThrow`, run-path rewiring, the dashboard-tool check; +~250 TS of tests (17 new cases) | The lenient `[]`/`null`/`"other"` fallbacks failed open on the run path: a trapped `statement_at` made Run Current and EXPLAIN ANALYZE skip the destructive check and run the whole buffer. The run path now uses throwing variants, tested by trapping single exports through a `callWasm` proxy (the reviewer's probe). AI dashboard widgets (`add_widget`, `update_widget`) now get `run_query`'s read-only check; before, the model's widget query was stored and run with no check at all.
- Task 11 (CI) | ~0.1 h (agent wall time) | +0 Rust | −0 TS; no workflow changes | Everything Task 11 lists was already in `ci.yml` from Tasks 1 and 7: `cargo test --workspace` runs the `seaquel-sql` parity tests, the wasm32 clippy has both crates, the frontend job installs wasm-bindgen at the `Cargo.lock` version, builds the module, runs vitest (GitHub sets `CI`, so the trap test fails rather than skips without the toolchain) and builds the demo, and the types check sees new generated files as untracked. The three workflows parse (PyYAML). The stale `pkg/` that `wasm:build` keeps locally can't happen in CI, the release runners or Docker: each starts without `pkg/` (gitignored, excluded by `.dockerignore`, not in rust-cache), so a missing toolchain fails there. Not yet run on GitHub.
- Task 12 (docs and cleanup) | ~0.6 h (agent wall time) | ±~40 lines of Rust comments (recorder paths now point at the artifacts; deleted TS files marked as deleted) | −~5,400 TS/JS (`scripts/sql-fixtures/`, 12 files) and all of `spike/` (93 MB with its build output); +14 artifact copies | The fixture README still told readers the recorder ran in `npx vitest run`, and CLAUDE.md still said `src/lib/db` held the statement splitting and `{{param}}` code. With the spike and recorder gone, `vite.config.js` needs no `test.exclude` at all. The design doc and the spike report were already off oxfmt's table formatting before this task; left as they were.
- Task 13 (measure) | ~0.4 h (agent wall time) | +0 Rust | −0 TS; design doc "Phase 2b cost" section, a status line and a Follow-up in the plan | (1) Line counts are a line diff of `crates/`, `src/`, `src-tauri/src`, `scripts/` and the root config files against the p2b-t0 snapshot, with fixture JSON, test files and top-level `#[cfg(test)]` modules split out; generated paraglide output (`src/lib/paraglide` and a stray `src/paraglide`) and `src/lib/wasm/pkg` left out. Rust +7,117 production, +3,508 tests, +129,404 fixture JSON; TS/Svelte production +1,240 −3,940 including the unchanged 126-line move of `pending-change-description.ts`. (2) Times summed from this log: ~14.5 h for Tasks 1–12, ~6.5 h of it review fixes and follow-ups (five of the lines are Task 2's). (3) The spike's `browser-check.mjs` read a `__seaquelSql` probe the shipped app doesn't have, so the adapted copy wraps `WebAssembly.instantiateStreaming`/`instantiate` from an init script and times the `.wasm` fetch start to the instance; the demo editor check counts calls through the same wrapper. Init medians: Chromium 3.9–4.2 ms, WebKit 22–26 ms. The desktop build opened in a plain browser also tries DuckDB-WASM's worker from jsdelivr, which the Tauri CSP refuses; that's the browser-mode provider, older than 2b and not on the wasm path. (4) The first `cargo test` run failed two Postgres smoke tests: `unique_checkbox_runs` drops every other `seaquel_uq_*` schema while two sibling tests are using theirs. The `--no-fail-fast` rerun passed (74 binaries, 571 tests), and so did 8 repeats of that binary alone.
