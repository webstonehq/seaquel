# AI safety effort log (task | wall time | Rust lines added | TS lines removed | surprises)
AI Task 1 | ~20 min | ~1,490 Rust lines added (≈640 outside tests, 440 of them the testkit harness; ≈850 in tests), 62 changed | 0 TS lines removed (52 added: interface + 3 stubs) | `QueryOptions` is `#[non_exhaustive]` with `with_read_only()`, so the 11 existing `query_stream` test calls got `QueryOptions::default()` appended; `fetch_capped` on `&mut *conn` checked with a throwaway Postgres arm (reverted); the harness needed `tokio` (time) as a normal testkit dependency for the cancel timing
AI Task 7 | ~40 min | 0 Rust lines | 68 TS lines removed (~900 added: ~265 outside tests, ~635 in tests incl. two new test files) | pendingApproval needed the connection type for the hint; the widget editor preview and the version diff view also ran widget SQL through `executeRaw`, so both moved to `DashboardManager.runWidgetQuery`; paraglide output had to be recompiled for `npm run check` to see the new keys; no persisted dashboard/widget connection binding (the plan keeps it a Follow-up)
AI Task 2 | ~35 min | ~925 Rust lines added (≈205 in the two drivers, ≈720 in the two new live test files), 12 changed | 0 TS lines removed | The plan's MySQL mechanism let a `CALL` through: a procedure that runs `SET SESSION transaction_read_only = 0` before its INSERT wrote on both servers, because a procedure runs each statement as its own; `START TRANSACTION READ ONLY` on top of the session setting stops it, and a procedure that first `COMMIT`s is a new accepted gap (fix 14 blocks `CALL`). Postgres's sqlx error text has no SQLSTATE, so mapping 25006 needed a small `Executor` wrapper that notes it for `fetch_capped`. The attacks passed on the first run of the implementation; `CREATE TEMPORARY TABLE` is refused on both MySQL and MariaDB
AI Task 5 | ~120 min (incl. the coordinator's switch to a per-call connection and two rounds of review fixes) | ~1,250 Rust lines added (≈330 in driver.rs/session.rs/bind.rs, ≈150 unit tests, ≈770 in tests/read_only.rs and read_only_row_cap.rs), 15 changed | 0 TS lines removed | The plan's release_state after ROLLBACK leaks session state (probed: SET CONTEXT_INFO, sp_set_session_context, a Session app lock and a GLOBAL cursor all outlive ROLLBACK and sp_executesql), so each call runs on its own short-lived connection (cap of 4 at once) and the user's held session is never touched; SET IMPLICIT_TRANSACTIONS ON + BEGIN makes the transaction two deep, so `COMMIT; DELETE`, `ROLLBACK; INSERT` and a proc that commits once are now rolled back (not committed as probed); 266 is never raised with IMPLICIT_TRANSACTIONS on, so escapes are detected from CURRENT_TRANSACTION_ID(), @@TRANCOUNT depth and a #temp marker table created in the transaction; remaining gaps (two COMMITs, a proc committing until @@TRANCOUNT = 0, NEXT VALUE FOR) pinned by a test; needs SQL Server 2016+ (CURRENT_TRANSACTION_ID); review: XACT_ABORT OFF and LOCK_TIMEOUT 10000 in the begin batch, a 60 s call timeout so blocked calls can't starve the 4 slots, and past the row cap the response is drained so escapes are still reported; re-review: errors are judged in a TRY/CATCH around a nested sp_executesql (the query's own error when the transaction id is unchanged, an escape otherwise), since 245/241/9400/13609 roll the transaction back even with XACT_ABORT OFF and option (a) had turned them into escapes
AI Task 4 | ~50 min | ~790 Rust lines added (≈130 in driver.rs, ≈640 in tests/read_only.rs; the typed-cell cases and SeededCopy moved from values.rs/smoke.rs into tests/common, ~330 lines moved, not new), 17 changed | 0 TS lines removed | `arrow_lossless_conversion` is global, so the clone inherits it; `SELECT * FROM checkpoint()` runs inside the read-only transaction and folds the WAL into the file (accepted: no data changes; the statements and `force_checkpoint()` are refused); a SELECT autoinstalls known extensions (`current_setting('TimeZone')` downloaded icu into the extension directory), part of the egress gap; CREATE SECRET and ATTACH ':memory:' inside a read-only transaction leave nothing after ROLLBACK even without `query()`; a failed statement in a DuckDB transaction aborts it, so a user transaction test can't probe with a nested BEGIN
AI Task 6 | ~20 min | ~320 Rust lines added (≈65 in stream.rs and commands.rs, ≈255 in the new server test), 10 changed | 71 TS lines removed (incl. the 3 Task 1 stubs; ~730 added: ~175 outside tests, ~490 in two new test files; 9 JS lines added to server.js) | The WebSocket handler only noticed a closed socket on its next send, and a read-only query sends nothing until it's done, so a web Stop never reached it: the handler now `select!`s on the socket too (test fails without it). server.js flushed a buffered first frame to Rust after the browser had already gone if Rust's socket was still connecting; fixed. DuckDB-WASM 1.32 runs a prepared statement to completion in its worker and `cancelSent` only interrupts `conn.send` (no parameters), so the demo keeps `query(?)` bound and an abort rejects at once while the query finishes in the background. DuckDB-WASM's Node build writes to the real filesystem (a naive run left `COPY`/`EXPORT` output in the repo root), so the demo's file attacks aim at a temp dir. `query()` names duplicate columns `a`, `a_1`
AI Task 3 | ~55 min | ~800 Rust lines added (≈290 in read_only.rs, 145 of them gate unit tests; ≈160 in driver.rs, 60 of them a lock test; ≈345 in tests/read_only.rs) | 0 TS lines removed | sqlx creates new files in WAL mode, so the journal_mode trace expects `wal`; a NUL byte ends SQLite's parse but makes sqlx loop forever on the zero-length tail (the gate refuses NUL, and the plain `query` path still hangs on it); a dropped query kept running and holding its locks until a progress handler (sqlx's safe `set_progress_handler`, no extra unsafe) interrupted it; shared-cache SQLITE_LOCKED is waited out by sqlx's unlock_notify, not returned as an error; the `ATTACH …; CREATE TABLE a.x` attack fails at prepare (QUERY_ERROR), not READ_ONLY
AI Task 2 review fixes | ~15 min | ~45 Rust lines added (PG rollback only on success + timed over-cap test, MySQL `SET SESSION TRANSACTION READ ONLY`, 2 blocked functions), comments and plan rows reworded | 0 TS lines removed | the over-cap test took 4.6 s with the old ROLLBACK and 0.17 s with the fix (20M streamed rows, 100k cap)
AI Task 3 review fixes | ~30 min | ~330 Rust lines added (≈100 authorizer + mapping in read_only.rs, ≈95 the NUL-refusing `SqliteDriver` wrapper in driver.rs, ≈135 tests) | 0 TS lines removed | `sqlite3_stmt_readonly` is true for process-wide PRAGMAs (hard_heap_limit crashed the process), so the read-only connection got an authorizer; `PRAGMA table_info(t)` passes its argument as the authorizer's value arg, so the allowlist has an "argument allowed" half; `pragma_*()` functions prepare their PRAGMA at step time, through the same authorizer; the NUL refusal needed a hand-written `Driver` wrapping the macro's driver (the macro is not ours to edit); the error code still comes from sqlx's `(code: N)` text, because `fetch_capped` has already turned the `sqlx::Error` into a `DbError`
AI Task 4 review fixes | ~35 min | ~260 Rust lines added (≈40 in driver.rs: per-call clone + LIMIT wrapper; ≈80 in seaquel-sql read_only.rs, 60 of them tests; ≈140 in tests/read_only.rs), ~20 changed | 0 TS lines removed | enable_profiling() on the shared clone outlived ROLLBACK and rewrote its file after every later call (caught by a negative control on the old code); enable_logging() is database-global, so only the token check can stop it, and query('…') had to be blocked on DuckDB because the check can't read its argument; without the LIMIT, range(2000000000) took 15 s to hit RESULT_TOO_LARGE; SUMMARIZE is refused by the first-word rule, not by SET, and SELECT * FROM (SUMMARIZE t) passes
AI Task 4 re-review fixes | ~20 min | ~60 Rust lines added (≈15 blocked names + doc, ≈45 tests) | 0 TS lines removed | the duckdb_functions() sweep (all autoloadable extensions + ui, MotherDuck, DuckLake, 1,389 names) found more than json_execute_serialized_sql: mysql_execute('my', 'CREATE TABLE …') created a table from the read-only path (the scanners' SQL runs outside DuckDB's read-only transaction), and start_ui_server() started the UI's HTTP server
AI Task 8 | ~30 min (about 13 of it the full check list) | ~25 Rust lines (the final review's two code items: 4 changed in the `query_read_only` doc comment, ~20 added for SQL Server's `log_read_only_failure`, which logs the model's own errors at debug) | 0 TS lines removed | no CHANGELOG exists, so the release notes went into the plan's execution notes as in phase 2b; the design doc wasn't oxfmt-clean before this phase and was left so; the log has no review-fix lines for Tasks 1, 6 and 7, and Task 5's ~120 min doesn't split first pass from review fixes
Totals | ~7.8 h logged (Tasks 1–7 ~7.3 h: first passes ~3.7 h for Tasks 1–4, 6 and 7, Task 5 ~2 h with its reviews, review fixes for Tasks 2–4 ~1.7 h; Task 8 ~0.5 h) against ~10.5–15.5 h in the plan and 5–9 h in the design doc | ~2,170 Rust production lines added (~1,600 plus the ~570-line testkit harness; ~550 of the ~1,520 non-blank ones are comments), ~4,960 Rust test lines (~330 of them moved) | ~170 TS/Svelte production lines removed, ~740 added; ~1,350 TS test lines added; 9 JS lines in server.js | bugs: 10 found by implementers, 16 by reviews (critical: SQLite process-wide PRAGMAs, DuckDB logging/profiling/scanner writes, SQL Server slot starvation and errors reported as escapes); final whole-phase review: ready, six minor items handled in Task 8
