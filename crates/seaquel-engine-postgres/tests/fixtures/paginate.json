{
  "cases": [
    {
      "name": "first page",
      "input": {
        "sql": "SELECT * FROM users",
        "limit": 100,
        "offset": 0
      },
      "output": "SELECT * FROM users LIMIT 100 OFFSET 0"
    },
    {
      "name": "later page",
      "input": {
        "sql": "SELECT id FROM orders ORDER BY id",
        "limit": 50,
        "offset": 150
      },
      "output": "SELECT id FROM orders ORDER BY id LIMIT 50 OFFSET 150"
    },
    {
      "name": "zero limit",
      "input": {
        "sql": "SELECT 1",
        "limit": 0,
        "offset": 0
      },
      "output": "SELECT 1 LIMIT 0 OFFSET 0"
    },
    {
      "name": "multiline query with trailing semicolon (appended as-is)",
      "input": {
        "sql": "SELECT *\nFROM users;",
        "limit": 10,
        "offset": 20
      },
      "output": "SELECT *\nFROM users; LIMIT 10 OFFSET 20"
    }
  ]
}
